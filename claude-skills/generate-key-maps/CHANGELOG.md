# Changelog

## 1.0.0 (unreleased)

- Initial implementation. Generates query-builder property modules
  (`<entity>.properties/index.ts`, `joins`, `functions/*.ts`, `COLUMNS.md`)
  for dashboard-api from a mikro-orm entity + frontend property file.
