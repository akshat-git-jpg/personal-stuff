# Execute plan 055 (heygen-web layered architecture)

You are a zero-context executor. Everything you need is in the plan file and the repo. Do exactly what the plan says — it is fully inlined (endpoint table, code skeletons, exact commands).

## Plan to execute
`plans/055-heygen-web-layered-architecture.md` — read the WHOLE file first, including its executor-instructions header, Steps, Done criteria, and STOP conditions.

## Before you start
1. Run the drift check: `git diff --stat 795e089..HEAD -- tooling/cli/heygen-web/ pipelines/youtube/tutorial-pipeline-1/lib/heygen.py pipelines/youtube/tutorial-pipeline-2/lib/heygen.py`. It should be empty or show only the two already-deleted `HANDOVER.md` files (they were removed on purpose; their content is re-homed into `CLAUDE.md` in Step 9). If anything else in `tooling/cli/heygen-web/` has drifted, STOP and report.
2. Work Steps 1–10 in order. The tree must keep working: `node tooling/cli/heygen-web/heygen-web.mjs help` must exit 0 after every runtime-code step.

## Load-bearing decisions — do NOT re-litigate
- **Request bodies must stay byte-for-byte identical to the current monolith.** The four payload JSONs and every HTTP body/header/query string are HAR-verified. Move code, never change what goes over the wire. If you cannot reproduce a body identically, STOP and report.
- **NO live HeyGen API calls.** Do not run `auth-check`, `generate*`, `studio-render`, `download`, `create-photo-avatar`, `usage`, `list-*`, or `photo-to-video` against the network. Verification is OFFLINE ONLY (`node --test`, `help`, import smokes). Live calls are ToS-grey and spend a real account — this is a hard STOP condition.
- **Do NOT edit the callers or secrets.** `pipelines/youtube/tutorial-pipeline-1/lib/heygen.py`, `tutorial-pipeline-2/lib/heygen.py`, `shared/heygen_config.py`, and anything under `infra/secrets/` are out of scope. Never read, print, move, or commit secrets.
- **Preserve exactly**: the entry path `tooling/cli/heygen-web/heygen-web.mjs`; the stdout JSON contract (machine output to stdout, human chatter to stderr); `help`/no-command exiting BEFORE `loadAuth()`; lazy auth (modules import without the secret file present); Avatar III defaults (`--iv`, `use_unlimited_mode`, `use_avatar_iv_model` unchanged).
- `API-REFERENCE.md` stays at the folder root — do not move it.

## CRITICAL — staging discipline (shared checkout)
This working tree contains concurrent, unrelated uncommitted work (e.g. `apps/founders-tracker/`, `tooling/claude-skills/debug-dbt-data-errors/`, `tooling/cli/local-apps-dashboard/`). It is NOT yours.
- **NEVER** run `git add -A`, `git add .`, or `git commit -a`.
- Stage only explicit in-scope paths: files under `tooling/cli/heygen-web/` and `plans/README.md`.
- Commit per step (rollback granularity), conventional-commit messages, no AI footers.
- **Do NOT push.**

## Run-log discipline — append to `plans/runs/20260709-2201-heygen-055.md`
- `[HH:MM:SS] PLAN 055 START` before you begin.
- `[HH:MM:SS] PLAN 055 HEARTBEAT <what just finished>` at least every 3 minutes / after each step.
- On success: `[HH:MM:SS] PLAN 055 DONE  verify: node --test -> pass  files: <changed paths>`.
- If blocked after 5 self-fix attempts on the same failure: `[HH:MM:SS] PLAN 055 BLOCKED: <reason>` and STOP the run.
- Final line: `[HH:MM:SS] RUN DONE`.
- When done, flip the plan 055 row in `plans/README.md` from `TODO` to `DONE`.

## Merge gate
`node --test tooling/cli/heygen-web/test/` must exit 0. That plus the Done-criteria commands in the plan are how you know you're finished.
