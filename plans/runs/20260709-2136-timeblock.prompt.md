# Executor handoff — run 20260709-2136-timeblock

You are a zero-context implementation executor. Implement ONE plan, exactly as written, then verify. You have NOT seen the conversation that produced the plan — the plan file is self-contained. Do not infer intent beyond it.

## Repo + branch

- Repo root: `/Users/kbtg/codebase/personal-stuff` (this is your working directory).
- You are ALREADY on branch `advisor/054-timeblock-day-planner`. Do NOT create or switch branches. Do NOT push. Stay on this branch the whole time.
- Note: this repo's operating rules are NOT auto-loaded for you — everything you need is in this prompt and the plan file. Do not go hunting in other folders.

## The plan

Read the ENTIRE file first, including every appendix (A–P): `plans/054-timeblock-day-planner.md`.

The appendices contain the FULL, verbatim contents of every file to create (the frontend HTML, worker TypeScript, tests, config, docs). Create each file with its appendix content exactly — do not redesign, reformat, or "improve" the code. The interaction and visual design were already approved; copy them.

Then execute the plan's **Steps 1 through 7 in order**. Run each step's **Verify** command and confirm the expected result before moving on.

**Drift check first** (expect empty output — nothing under apps/timeblock yet):
`git diff --stat aa0cea7..HEAD -- apps/timeblock`

## CRITICAL rules (read carefully — these prevent damage)

1. **Staging discipline.** Stage ONLY files under `apps/timeblock/` plus these four exact files when the plan says to edit them: `my-hosted-sites.md`, `INFRA.md`, `decisions.md`, `plans/README.md`.
   - NEVER run `git add -A`, `git add .`, `git add -u`, or `git checkout -- <file>` on anything else.
   - The working tree contains UNRELATED uncommitted changes (files under `apps/founders-tracker/`, `tooling/claude-skills/debug-dbt-data-errors/`, `tooling/cli/local-apps-dashboard/apps.json`). You MUST NOT stage, edit, revert, delete, or otherwise touch any of them. Leave them exactly as they are.
2. **All writes stay inside the repo.** Only create/modify files under `/Users/kbtg/codebase/personal-stuff`. Never write to the home dir, `/tmp`, or anywhere outside the repo. (`apps/timeblock/.dev.vars` is fine — it's inside the repo and .gitignored.)
3. **Commit per step** with clear conventional-commit messages, e.g. `feat(timeblock): scaffold worker + auth + KV day store`. No AI-mention footers, no co-author lines.
4. **Do NOT deploy.** Do not run `wrangler deploy`, `wrangler kv namespace create`, or `wrangler secret put`. Leave the KV `id` placeholder in `wrangler.toml` as-is. These need the owner's Cloudflare account (see the plan's STOP conditions).
5. **Skip the interactive screenshot.** Plan Step 5 asks for a browser screenshot — you are headless and cannot do that. Instead: create `apps/timeblock/.dev.vars` (test password, NOT committed) as the plan shows, then confirm the code is sound via the merge gate below. Do NOT start a long-running `wrangler dev` server and block on it — if you want to sanity-check that the worker bundles, a brief `npx wrangler dev` that you stop after a few seconds is optional, not required. The orchestrator will do the runtime smoke test.

## Merge gate (the hard requirement)

`cd apps/timeblock && npm install && npm run check` must exit 0 (that runs `tsc --noEmit` + `vitest run`). If `npm install` picks a wrangler/vitest major that fails to typecheck, install the current latest of the offending package and re-run (the plan's Appendix I note covers this).

**Cap self-fix at 5 attempts.** If `npm run check` still fails after 5 fix attempts on the same error, append `[HH:MM:SS] PLAN 054 BLOCKED: <reason>` to the run-log and STOP — do not keep looping.

## Run-log (append-only)

Append to `plans/runs/20260709-2136-timeblock.md`:
- `[HH:MM:SS] RUN START`
- `[HH:MM:SS] PLAN 054 START`
- `[HH:MM:SS] PLAN 054 HEARTBEAT <short note>` — every few minutes while working
- `[HH:MM:SS] PLAN 054 DONE  verify: npm run check -> <pass/fail summary>  files: <changed files>`
- `[HH:MM:SS] RUN DONE` — final line on success

## When done

- Update the `054` row in `plans/README.md` to `DONE`.
- Confirm `git status --short` shows ONLY your intended files as changed/committed (plus the pre-existing unrelated dirt, which must be UNCHANGED from before you started). Report anything unexpected instead of cleaning it up.
- Do NOT push. Stop and let the orchestrator verify.
