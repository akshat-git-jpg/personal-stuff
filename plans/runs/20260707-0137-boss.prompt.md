# Handoff: build the "boss" system (plans 039–042)

You are an executor. Build the `boss` system by executing four implementation plans
exactly. You have full repo context (this is the personal-stuff repo). Do not brainstorm
or redesign — the plans are the source of truth and every decision is already made.

## Branch and push rules
- You are ALREADY on branch `advisor/039-042-boss-batch`. Do ALL work on THIS branch.
- Do NOT create new branches. Do NOT `git push`. Do NOT open PRs. The orchestrator lands
  this branch via greenlight after verifying your work.
- Commit ONCE per plan (four commits total), message `feat(boss): plan <NNN> <slug>`.

## What to read first
1. `docs/specs/2026-07-07-boss-design.md` — the design (context).
2. Each plan, FULLY, before starting it:
   `plans/039-boss-core.md`, `plans/040-boss-agy-executor.md`,
   `plans/041-secretary-raise.md`, `plans/042-secretary-groom.md`.

## Order (strict — later plans depend on earlier files)
039 → 040 → 041 → 042.

## Per-plan procedure
For each plan, in order:
1. Run its **Drift check** command first (top of the plan).
2. Do its Steps in order. The scripts are inlined verbatim in the plans — create them
   exactly as written. Use the named captain files ONLY as read-only pattern exemplars;
   never modify `tooling/captain/**` or `tooling/cli/**`.
3. Run EVERY **Verify** command and confirm the expected result before moving on.
4. When the plan's **Done criteria** all pass, set its `plans/README.md` status row to
   DONE, then commit.
5. Append to the run-log `plans/runs/20260707-0137-boss.md`: a `PLAN <NNN> START` line,
   `HEARTBEAT` lines while working, and `PLAN <NNN> DONE  verify: <results>  files: <list>`
   (or `PLAN <NNN> BLOCKED: <reason>` then STOP the whole run). After plan 042 succeeds,
   append `RUN DONE` as the final line.

## Self-fix cap
Up to 5 fix attempts per plan. If a plan's Done criteria still fail after 5 attempts,
write `PLAN <NNN> BLOCKED: done criteria unreachable after 5 attempts` to the run-log and
STOP. Never loop indefinitely.

## Hard constraints (STOP and report if any is forced)
- Do NOT push, do NOT run `scripts/relink.sh`, do NOT deploy, do NOT edit files outside
  this repo, do NOT touch `tooling/cli/**` or `tooling/captain/{bin,lanes.d,state}` or any
  other existing skill.
- Verifies here are `bash -n`, `grep`, `test`, and the stubbed `tooling/boss/test-boss.sh`
  ONLY. Never launch a real `claude`, real `agy`, real `greenlight`, or a real `git push`
  to satisfy a Verify. `test-boss.sh` must stub every external command.
- Honor every plan's STOP conditions literally.

## Load-bearing decisions (do not re-litigate)
- Reuse `greenlight`, `wt`, `notify` from `tooling/cli/` — never reimplement them.
- boss shares NO code with captain (captain is frozen); build boss clean.
- Plans live in `plans/` — there is NO `plans/boss/` folder.
- GitHub labels are boss's live state; the plan's YAML frontmatter is boss's only routing
  input; `plans/README.md` is the durable record.
- The agy executor's `collect` MUST assert HEAD advanced (plan 040) — do not simplify it.
- Skill files (plan 041/042) are authored here, but `relink.sh` + session restart is a
  MANUAL post-merge step — you do NOT run it.

## Definition of done for the whole run
All four plans committed; `bash tooling/boss/test-boss.sh` prints `ALL TESTS PASSED`;
`bash -n` clean on every `tooling/boss/**/*.sh`; captain DEPRECATED banners in place;
`decisions.md` line appended; `RUN DONE` in the run-log.
