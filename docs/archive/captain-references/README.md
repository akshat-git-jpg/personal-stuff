# captain — archived references

`tooling/captain/` was deleted on 2026-08-23 (deprecated 2026-07-07, superseded by
`tooling/boss/`). Only these two research/reference docs were kept, because
`decisions.md` and `.claude/skills/personal-stuff-failure-archaeology` cite them:

- `antigravity-cli-findings.md` — the scout report that killed the gemini-headless lane
  and picked `agy` instead.
- `officer-brief-template.md` — captain's officer brief format, kept as the ancestor of
  boss's plan frontmatter.

Everything else (bin/, lanes.d/, data/, state/, CLAUDE.md, README.md, tests) is gone.
Recover from git history if ever needed: `git log --all -- tooling/captain`.
