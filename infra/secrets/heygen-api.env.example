# Copy to heygen-api.env (same folder, gitignored) and paste your key.
# HeyGen dashboard -> Settings -> API -> API Token.
#
# ⚠️ This key is METERED. Every render through tooling/cli/heygen-api bills
# credits. The free path is tooling/cli/heygen-web (captured web session).
HEYGEN_API_KEY=hg_replace_me
