# <ModuleName> — Module HLD

## Overview

Business use case overview, mention the common used terminology for this module. Correlate terminologies with PRD and zluri help articles (Ask your PM to describe the module in 2 to 3 sentences). Add Domain specific knowledge about the module.

### MongoDB Collections
List of collections in database and core responsibility of each collection

### Postgres tables Collections
List of postgres table and core responsibility of the tables, also mention the corresponding mongo collection if its 1:1 mapping.

## Relevant Repositories

| Repository | Responsibility | Repo-level docs |
|---|---|---|
| `<repo name>` | High level responsibility | `postgres/zluri-docs/docs/<module>.md` |
