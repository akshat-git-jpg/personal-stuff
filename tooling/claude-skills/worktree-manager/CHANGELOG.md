# Changelog

## 1.0.0 — 2026-04-06

- Initial release
- Creates git worktrees across multiple Zluri repos using worktrunk (`wt`) with git fallback
- Copies `.env` files with special handling for `dashboard-api/postgres/.env`
- Optional Cursor `.code-workspace` generation
- Memory-based parent folder caching (agent-agnostic)
- Generic path examples
