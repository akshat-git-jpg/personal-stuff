# hostinger

`pp-hostinger` CLI — manage the Hostinger VPS, domains, DNS, firewall, and
snapshots from the command line (used by the `hostinger` Claude skill instead of
an MCP server). Entry point `pp-hostinger` (wraps `pp_hostinger.py`).

<!-- stub: flesh out with the command list + auth setup when convenient. -->
