#!/usr/bin/env bash
# Thin wrapper so `pp-trip …` runs the Python entry-point with the repo's python3.
set -euo pipefail
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python3 "$DIR/pp_trip.py" "$@"
