# sheets

`pp-sheets` CLI — read and write Google Sheets from the command line (used by
the `google-sheets` Claude skill instead of an MCP server). Auth via `auth.py`;
entry point `pp-sheets` (wraps `pp_sheets.py`).

<!-- stub: flesh out with the command list + auth setup when convenient. -->
