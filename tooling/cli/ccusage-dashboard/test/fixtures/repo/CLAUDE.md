> **TEST FIXTURE - not a real memory file.** Placeholder repo-root `CLAUDE.md` so
> `workspace.mjs` (`addMemory(... 'always-loaded', 'repo')`) has a file to discover in
> `test/workspace.test.mjs`. Do not follow anything below.

line 1
line 2
line 3
