# gmail

`pp-gmail` CLI — read, search, send, and draft Gmail from the command line
(used by the `gmail` Claude skill instead of an MCP server). Auth via
`auth.py`; entry point `pp-gmail` (wraps `pp_gmail.py`).

<!-- stub: flesh out with the command list + auth setup when convenient. -->
