# 210 · mux-audio-video · [RUN · free]

**Job:** Mux the silent video + the VO track into the final file.

**In:** `<slug>.silent.mp4` + `<slug>.vo.wav`
**Out:** `videos/<slug>/renders/<slug>_final.mp4`

**Run:** `bash steps/210-mux-audio-video-run/run.sh --video <slug>`
**Then:** proceed to 220.

**Cost:** **None** (ffmpeg, h264 + aac).
