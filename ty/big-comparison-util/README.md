# big-comparison-util

A standalone grouping-and-ranking method for comparing many tools at once: group a tool list into category buckets, then pick per-category and overall winners (web-research-first, then user input).

- `categorization-rule.md` — the rules doc. **Deliberately not a skill** — not auto-discovered, not symlinked. Point Claude at this file each run.

> Stub scaffolded by `/audit-repo-route` — flesh out as needed.
