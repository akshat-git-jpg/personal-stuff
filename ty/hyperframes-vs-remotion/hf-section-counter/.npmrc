# Force public npm registry for this project.
# Home ~/.npmrc defaults to Zluri CodeArtifact (private, token expires) which
# 401s on public packages like hyperframes. npm only reads the .npmrc in the
# command's cwd, so it must live here (TY/.npmrc one level up is not picked up).
registry=https://registry.npmjs.org/
always-auth=false
