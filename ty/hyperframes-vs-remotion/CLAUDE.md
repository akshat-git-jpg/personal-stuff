# hyperframes-vs-remotion — prior experiment

A one-off comparison of two HTML→video approaches (Hyperframes vs Remotion). Kept for reference.

**The live tool is [`../yt-visuals-hyperframe/`](../yt-visuals-hyperframe/README.md)** — that's where current video-card work happens, and it's what `personal-stuff/apps/hyperframes-render` (render2.agrolloo.com) reads from. Don't build new work here.

Subfolders:
- `hyperframes/` — the Hyperframes side of the experiment (see its `CLAUDE.md`).
- `yt-visuals/` — the Remotion side (see its `CLAUDE.md` and `STATUS.md`).
