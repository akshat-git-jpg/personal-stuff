APP_PASSWORD=changeme
SESSION_SECRET=change-me (a random string, 32+ chars)

# Google OAuth — copy these three from the shared token that routine-ringer and
# the daily-digest already use:
#   tooling/mcp/google-shared  (or apps/telegram-my-planner/tools/daily-digest/token.json)
# The token already carries the https://www.googleapis.com/auth/calendar scope.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REFRESH_TOKEN=
