{
  "name": "timeblock",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": ". ../../scripts/node22-path.sh && wrangler dev",
    "typecheck": ". ../../scripts/node22-path.sh && tsc --noEmit",
    "test": ". ../../scripts/node22-path.sh && vitest run",
    "check": ". ../../scripts/node22-path.sh && tsc --noEmit && vitest run",
    "deploy": ". ../../scripts/node22-path.sh && wrangler deploy"
  },
  "dependencies": {
    "hono": "^4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4",
    "typescript": "^5",
    "vitest": "^2",
    "wrangler": "^3"
  }
}
