{
  "name": "redirector",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": ". ../../scripts/node22-path.sh && wrangler dev",
    "deploy": ". ../../scripts/node22-path.sh && wrangler deploy",
    "test": ". ../../scripts/node22-path.sh && npm install && vitest run --passWithNoTests",
    "typecheck": ". ../../scripts/node22-path.sh && npm install && tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0",
    "wrangler": "^3.95.0"
  }
}
