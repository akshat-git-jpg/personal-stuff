# yt-script-desk — Claude Code guide

Run `npm run dev:local` for the local Vite + Node server pair, `npm test` for the Vitest suite, and `npm run shot` to regenerate the committed screenshots under `docs/shots/`. The left track only ever renders spoken copy (`say`) or the maker's own typed text — never `show`/`edit`/`facts`/`angle`; see the root `decisions.md` (2026-08-18) and `plans/232-script-desk-write-view.md` for why. Full operating notes land with plan 235.
