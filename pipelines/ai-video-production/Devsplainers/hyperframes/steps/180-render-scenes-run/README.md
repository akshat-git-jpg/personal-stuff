# 180 · render-scenes · [RUN · free]

**Job:** Render each scene to a silent MP4.

**In:** `videos/<slug>/scenes/*/index.html`
**Out:** `videos/<slug>/renders/scenes/sNN.mp4`

**Run:** `bash steps/180-render-scenes-run/run.sh --video <slug>`
**Then:** proceed to 190.

**Cost:** **None** (Hyperframes render).
