# 080 · write-storyboard · [OPUS · critical]

**Job:** Turn beats + durations into the visual plan: per-scene metaphor, layout, kit atoms, and the color law. Include a machine `scenes` block (one "NN slug" per line) that 090 reads.

**In:** `videos/<slug>/beats.md` + `videos/<slug>/durations.json`
**Out:** `videos/<slug>/storyboard.md` (human table **+** a fenced ```scenes block)

**Run:** — Opus authors it directly (the second and last step that needs Opus).
**Then:** proceed to 090.

**Cost:** **Opus.** Defines how every scene looks — Antigravity executes this plan, so its quality sets the ceiling.
