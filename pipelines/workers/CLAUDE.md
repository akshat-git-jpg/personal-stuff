# workers — Cloudflare Workers

Container folder. One subfolder per Worker. Each Worker is its own deployable Node project with its own `package.json`, `wrangler.toml`, and `node_modules/`.

## Workers

- `redirector/` — short-link redirector for `go.agrolloo.com/*`
