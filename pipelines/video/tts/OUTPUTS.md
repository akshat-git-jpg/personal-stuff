# Generated voiceovers — manifest

The tracked record of every voiceover generated by this hub. The audio itself lives OUTSIDE
the repo (media policy) in `~/kb-scratch/video/tts/<pipeline>/` — `_test/` for runs tied to
no pipeline. Append a row per generation.

Pre-hub outputs (bench runs in the old `tts-flow/work/*`, `~/Desktop/voiceover_v2_chunked_trimmed.mp3`)
predate this manifest and are not retro-logged.

| Date | Output file (under ~/kb-scratch/video/tts/) | Pipeline | Engine | Ref voice (slug) | Source / notes |
|---|---|---|---|---|---|
