# references/

A committed directory storing one `<video-id>.md` per analyzed video.
These reports contain moment tables and effect candidates discovered via `analyze-reference.sh`, feeding into `EFFECTS.md` recipes.
