# Graphics-flow test log

One section per test video. The working data for each test lives in
`videos/<slug>/` (text committed, media gitignored); this file records what was
tested, with what source, and what the run taught us.

## test-01 — first end-to-end dry run (2026-07-18)

- **Source**: Drive folder `1H2Ffkqw_xWMUR20EWLWQ7ydTGAQ-rZoL` (owner: khushibakliwal251) —
  3 tutorial segments: `intro.mp4` (86.7s), `BODY .mp4` (1683.0s), `conclusion.mp4` (158.0s).
- **Workdir**: `videos/test-01/` (raw segments in `src/`, gitignored).
- **vo.mp3**: audio of the 3 segments concatenated in order intro → body → conclusion;
  total 1927.6s (32:08). Segment boundaries on the VO timeline: intro 0:00.0,
  body 1:26.7, conclusion 29:29.7. `offset` left at 0 (timeline = concatenated segments).
- **Goal**: prove the full loop on real content — transcription quality (product names!),
  cue-pass anchor discipline (Sonnet), resolver hit-rate, board review usability,
  capacity limits vs real content, render + manifest.
- **Status**:
  - [x] segments downloaded, audio concatenated
  - [x] 010 transcribe — local whisper small.en, ~8 min for 32:08 audio; 6110 words
  - [x] 020 cue pass — Sonnet subagent, 21 cues, 0 flagged (~109k subagent tokens)
  - [x] 030 resolve — 21/21 cues + all beats matched FIRST TRY, zero anchor misses
  - [x] 040 board review (owner) — approved 2026-07-18 after two feedback rounds (v2 fold: 27→18 cues)
  - [x] 050 render + manifest — 2026-07-18, first full run: 18/18 clips, all ffprobe-verified, manifest.md written
- **Findings**:
  1. **ASR garbles brand names** (HeyGen → "Heigen/Hazen/Haitian", OpenArt → "OpenARC").
     Anchors still resolved (they quote the garbled transcript verbatim — by design), but
     Sonnet copied garbled spellings into on-screen reveal text. Fixed post-hoc in display
     fields only (9 fixes). TODO: rulebook rule — reveal/variable text uses CORRECT brand
     spelling; only anchors stay transcript-verbatim; cue-pass prompt should take an
     optional product-name list from the video's dossier.
  2. **Local whisper is slow** (~8 min). FIXED same day: step 010 now tries Groq
     whisper-large-v3-turbo first (lib/transcribe-groq.mjs; 22s for this file, 6092
     words, word granularity, 25MB limit handled by 16kHz-mono downsample), local
     whisper stays as fallback. Bonus: Groq spells the brand names CORRECTLY
     (HeyGen/OpenArt/Higgsfield — zero garbles), largely dissolving finding #1 for
     future runs; timestamps agree with local within 0.77s on sampled words.
  3. Anchor discipline held: Sonnet self-verified verbatim+ordering; resolver confirmed.
  4. (renumbered below)
  5. Board port clash: a stale board instance from plan-065 verification held :4322;
     my launch died EADDRINUSE while the stale one served fixture data. TODO: board
     should fail with a clear "port in use" message and/or auto-pick a free port.
  6. **Catalog under-specified array shapes — TWO broken cards on the board** (owner
     screenshots): bullet-points-highlighted rendered "undefined" per row (card wants
     items:[{label}], catalog said "array", Sonnet sent strings) and summary-table
     staircased (rows need products.length score values + a trailing reason cell;
     catalog said values:"array", Sonnet sent 5 scores, 6 cells hit a 7-col grid).
     FIXED same day at three levels: both cards normalize LLM-shaped input
     (strings→{label}; pad/trim rows + separate reason field), catalog.json array
     types now spell out exact item shapes for all 8 affected cards, and this class
     of bug feeds the auto-QC design (schema validation + frame inspection).
  7. (v1) Sonnet judgment notes: inferred one unstated score (OpenArt "Creative Ground" = 5);
     dropped a 6-item feature list (items too short for 3-word anchors); moved the CTA
     cue off the last-20s zone. All reasonable — review on board.

### v2 cue pass (2026-07-18) — new 42-card catalog + routing rules

- Re-ran step 020 (Sonnet again, for comparability) after the card batch + specificity/repetition rules.
- **27 cues, 0 flagged**: 5 stat-hit (100 models / 70 presets / 240 avatars / 140 languages / 300 actors), 3 tool-intro + 2 section dividers (cap respected via substitution), 3 step-flow, 3 credits-math, persona-match payoff (5 beats), summary-table trimmed to the 4 fully-stated factor rows, callout styles varied. The v1 monotony (5x bullet-list + 5x identical callout) is gone.
- Validation/resolver caught 5 real errors across two rounds (3 beat anchors placed at/before their cue anchor — the monotonic cursor can't reach backward; 2 fullframe overlaps from heuristic holds). Fix loop: resolver errors + transcript snippets sent back to the SAME warm subagent (one small round), plus one negative-lead fix (c06 lead -1.1 — a single card's duration is catalog-fixed, so overlap resolves from the next cue's side).
- Learnings for the rulebook (future): beat anchors must come AFTER the cue anchor; single fullframe cards squeezed against a next cue can use negative lead.
- v1 cues.json remains in git history (committed 2026-07-18 before overwrite).

### v1 shot pass (2026-07-18, late night) — first avatar shot plan (pilot)

- Ran step 070 in-session (Fable) after 077–081 landed: 9 avatar-full spans,
  247.7s/300s cap, longest host-less gap 278s (under the new 300s cadence rule).
- Fix loop: 2 lint errors in round 1 — E2 (intro span clipped the toc card by
  0.5s; to_anchor pulled one phrase earlier) and E3 (Higgs verdict resolved
  9.8s < 12s min; to_anchor extended). Round 2 clean, zero warnings.
- **Owner approved with ZERO edits** (shots.llm.json == shots.json). Submit ran
  same night: `girl-1`, `--spans-only` (corner deferred), HeyGen 3 templates;
  s03 failed on first submit attempt (transient — neighbors succeeded), retry
  via idempotent re-run.

## Folded lessons

- 2026-07-18 — owner: 5 stat-hits in one video read as a tic → RULEBOOK+prompt: stat-hit max 3/video, ≥90s apart, keep the most impressive numbers. (First fold via steps/060.)
- 2026-07-18 — owner: no at-a-glance view of template mix → board header now shows per-card usage chips (red when >3).
- 2026-07-18 — owner: tools should show real logos/icons, text-only names click less → OPEN, needs design decisions (asset source, which cards) before building.
- 2026-07-18 — owner: lint banner has no dismiss and stays forever → board banners now carry a ✕ (client + server-rendered).
- 2026-07-18 — session: port-walk printed the STALE port in "board at" (failed listen()'s success callback stays registered and fires first) → resolve from server.address().port, not the closure.
- 2026-07-18 — owner (board review v2): step-flow over on-screen demos is redundant → removed c06/c09/c15; RULEBOOK+prompt step-narration rule (no graphic over actions the recording shows).
- 2026-07-18 — owner (board review v2): tool openers must be ONE consistent card, not a mix forced by the repetition cap → all 5 on section-counter-scale; structural-consistency rule (RULEBOOK+prompt), `structural: true` in catalog, lint E3 exempts structural cards.
- 2026-07-18 — owner (board review v2): per-tool pricing graphics out, one pricing comparison at the end → removed c20–c24, added c28 (summary-table with real VO-stated prices + logos); pricing-consolidation rule (RULEBOOK+prompt).
- 2026-07-18 — owner (board review v2): "why no tool icons" → logos applied across test-01 (openers/stat-hits/tables/persona-match); section-counter-scale gained an optional logo slot (rendered + inspected); no new rule (068's rule already covered it — test-01 predated the registry).

- 2026-07-18 — owner: "video must not run long without the full-screen host — periodic presence, not just start/end" → `GAP_AVATAR_MAX=300` + W4 span-cadence in lint-shots.mjs; RULEBOOK priority 5 rewritten from "mid-demo last resort" to cadence beats.
- 2026-07-18 — owner (shot-plan board review): shot-block JSON editor was an unstyled white sliver + avatar lane was nested INSIDE the graphics minimap → styled shot-frag, separate labeled lanes + color legend.
- 2026-07-18 — owner: lane colors all read as similar warm hues → distinct hues (orange fullframe / sky-blue overlay / violet avatar; green stays reserved for approved states).
- 2026-07-18 — owner: "why two approve buttons?" → kept two gates (independent lifecycles, cue-edit auto-un-approves shots), renamed bare "Approve" to "Approve graphics"; one review sitting covers both.
- 2026-07-18 — session (s03 retry): avatar-jobs.json was only flushed inside the submit branch, so a retry whose trailing jobs all skip dropped s04–s09 from the file (video_ids recovered from RENDERS.md — the CLI's auto-append row per submit is the designed backup, and it worked) → final unconditional flush after the loop + regression test.
- 2026-07-18 — owner asked WHY s03 failed → unanswerable: the submit swallowed the CLI's stdout/stderr (empty catch), losing both the error AND the per-submit ✓UNLIMITED meter proof → failed jobs now record an `error` field + per-job stderr lines incl. meter verdict, ⚠️ loud warning on NOT-free; `usage --save` baseline saved so the next run's meter diff is provable. Failure itself was transient (neighbors + retry succeeded on the same session — not auth, not ban, not quota).

- 2026-07-25 — owner (test-01 Final Cut v2 :3) "Workflow and Pricing not syncing with voice, pls tell long term fix" → root cause: `findFrom` in resolve.mjs only ever searches FORWARD, so a beat anchor whose words recur later in the script silently matched the later copy and the reveal fired against the wrong sentence. Fixed with `BEAT_GAP_MAX=15` (cue-constants, so it reaches the prompt): consecutive beats resolving more than 15s apart are now a resolver ERROR naming the anchor, instead of drifting silently.
- 2026-07-25 — owner (test-01 Final Cut v2 :5) "Why green screen? Is the template wrong?" → the template was right; visuals-flow was wrong. `link-in-description` is authored as a chroma-key overlay (`--bg: #00b140`) for the render2 editor, but assemble.mjs composited it with a bare `overlay=` filter and never keyed, so a green plate shipped over the footage. Fixed WITHOUT touching the shared card (render2's Templates tab depends on its green): catalog gains a `chroma` field, resolve carries it, assemble applies `colorkey` when present.
- 2026-07-25 — owner (test-01 Final Cut v2 :6) "have this screen left aligned only like other screens of same template. Don't switch" → root cause: resolve.mjs rotated `variants` by use-count for EVERY card, so `section/tool-intro` (`structural: true`) alternated layout a/b/a/b across the five tool sections. Structural cards now pin `variants[0]`; an explicit `variables.variant` still wins. Consistency is the entire point of a structural card.
- 2026-07-25 — owner (test-01 Final Cut v2 :9) "subtitles way out of place, spelling mistake, random words highlighted" → three separate bugs in captions.mjs, all fixed: (a) Whisper splits one-word brands ("Higgs Field"), so a `CAP_SPELLING_FIXES` lexicon now merges the pair back into one word spanning both timings, pre-chunk; (b) `markKeyword` highlighted EVERY ALL-CAPS token, lighting up "AI" in almost every caption of an AI-tools video — conventional acronyms are now stopworded; (c) it highlighted any token containing a digit, so every "5" and "3" glowed — restricted to money, percent, and 2+ digit figures.
- 2026-07-25 — owner (test-01 Final Cut v2 :11) "what is the criteria to decide when to show this text motion graphics, this was not at all a appropriate time" → c19 gave the whole frame for 3.6s to "So let's talk about the good ones first", a discourse marker with no claim in it, mid product-walkthrough. R_KINETIC gains a substance clause and lint E11 no-filler-slate rejects navigational openers on `kinetic-sentence` / `keyword-statement`. Test in the rule: if you cannot pick an `accent` that states the point, there is no point, so there is no card.
- 2026-07-25 — session (test-03 run): **the 035 audit gate had never gated anything.** `audit-gate.mjs` built its cue map from `resolved.cues`, but `resolve.mjs` writes the array under `resolved`, so the map was always empty, every `resolvedMap.get(id)` missed and `continue`d, and the gate returned `{errors:[],warnings:[]}` for any input — including an unaccepted labelled fullframe, the one thing it exists to block. Fixed to accept `resolved` / `cues` / a bare array, and to ERROR when it finds no cue array at all, so it can never silently go quiet again. Three regression tests added. Worth remembering: a gate that always passes looks exactly like a gate that never fires.
- 2026-07-25 — owner (test-03 Final Cut v2 :2 and :5) "this motion graphic ends before he completes the sentence ... such timing sync gaps keep happening, need a better solve" → root cause: a single card's duration was `cat.default_duration`, a CATALOG CONSTANT with no relationship to the clause it illustrates (beat cards used lastBeat+hold, equally blind), and `extendExposure` only added time when the NEXT card happened to fall within 12s. Nothing in the chain asked where the sentence ended. c03 died 7.4s before its sentence; c10 died 2.1s into "Still, neutral so far". Fixed with `sentenceEndIfMidSentence` + `EXPOSURE_TAIL`: a fullframe card may never exit mid-sentence. **Measured from the card END, not its anchor** — the first attempt measured from the anchor's sentence and did NOT fix v2:5, because that card was anchored two sentences earlier than where it exited.
- 2026-07-25 — owner (test-03 v2 :4) "for one tool you showed the intro then the screen recording, but for the other the intro stayed long and there was no screen recording" → NOT a request for equal durations (owner explicitly corrected that reading); a request for consistent section SHAPE. Root cause: `extendExposure` granted c09 +8.4s because the next card was 8.4s away (≤ GAP_ABSORB), consuming the entire footage window, while c08's 22.6s gap exceeded the threshold and survived untouched. Structural cards now reserve `SECTION_FOOTAGE_MIN` from absorption, plus lint W11. Per-section durations stay free to differ — only the shape is held.
- 2026-07-25 — owner (test-03 v2 :4 follow-on): structural section openers are also EXEMPT from sentence-aware exposure. The OpusClip opener sits inside a 24-second run-on sentence; holding it for the whole sentence swallowed the 22.6s of screen recording the owner had just praised. Openers announce and hand over; content cards hold their thought.
- 2026-07-25 — owner (test-03 v2 :1) "why only Submagic is coloured and not Opus Clips" → `markKeyword` tests ONE word at a time, so a brand the speaker says as two words never matched. Traced to the same day's transcript QA, which deliberately kept "Opus Clips" as spoken rather than merging it to "OpusClip". Fixed with `CAP_ACCENT_PHRASES` — every word of a multi-word brand highlights, words stay separate, captions still read as spoken.
- 2026-07-25 — owner (test-03, chat) "I don't think you are ever proposing the new templates which we should be making. You are always trying to use existing templates." → confirmed by this run's own audit: c02 was marked `labelled, accepted` with the note "no enacted device fits", which is exactly the moment to propose a card. R_CHOOSING gains a mandatory propose-a-new-card clause, and two cards were built from this round's feedback (`enacted/promise-split`, `link-in-description/link-scrim`). The catalog is meant to grow with the videos.
- 2026-07-25 — session (test-03): **flagging a span does not un-plan its avatar job.** `avatar-jobs.json` is planned from `shots.resolved.json` at submit time; flagging s01 afterwards updated shots.json and shots.resolved.json but left the job in place, and `assemble.planSegments` builds base segments from the JOBS — so the flagged span still claimed screen time and threw "overlapping base segments: c01 ends 15.26, s01 starts 10.6". The jobs file needs pruning against live spans whenever a span is flagged after submission. Candidate real fix: have assemble filter avatar jobs through `shots.resolved.json` rather than trusting the jobs file.
- 2026-07-25 — session (test-03): **changing cue timings invalidates the shot plan.** Extending the cold open and giving promise-split its two sentences turned 0.6-39.6s into unbroken fullframe graphics, and avatar span s01 (10.6-22.4s) no longer had anywhere to sit — lint E2 caught it as a fullframe collision. Re-run `run.sh <slug> shots` after ANY cue-timing change; the shot plan is downstream of cue geometry even though it is authored before the gate.
- 2026-07-25 — session (test-03 run): **every register transition has been planned and then silently dropped.** `effects-plan.mjs` built `conceptSpans` from concept.json and wrote `whip-reg-*` instances into effects.json; `assemble.mjs` built its ctx WITHOUT `conceptSpans`, re-planned, produced no `whip-reg-*`, and therefore treated the manifest's own entries as unknown ids — `warning: ignoring effects.json instance with unknown id: whip-reg-30.7`. The dark↔light lift/drop the whole concept pass exists to drive has never rendered. The identical warning on test-01 (`whip-reg-45.9`) was written up in the handoff as a "harmless leftover from the before-after card removal"; it was not, and it reproduced on a fresh video with no history. Span loading extracted to `lib/concept-spans.mjs` and used by both callers so they cannot drift again. Lesson: a warning that names a specific stale-looking id is worth reproducing on a clean video before calling it leftover.
- 2026-07-25 — session (test-03 run): lint W9 variant-rotation contradicted the same day's structural-variant fix — it warned whenever consecutive cues shared a card and variant, which is now the *intended* result for `structural: true` cards pinned to `variants[0]` (the two `section/tool-intro` openers). W9 now exempts structural cards. A rule folded on one surface can contradict a rule already living on another; check the linter when changing the resolver.
- 2026-07-25 — session (test-03 run): a cue anchored at the exact opening words of a register span starts `lead` seconds EARLIER and therefore lands in the previous span, tripping E8 concept-register. Hit three times in one file. Not yet fixed in code — the workaround is to start each register span a sentence earlier than the cue that opens it. Candidate real fix: compare the cue's ANCHOR time against spans rather than its lead-adjusted start.
- 2026-07-25 — session: the previous round marked fixes in `claude_status.json` (which the board renders as green check-offs) but never set `applied`/`folded` in feedback.json, which is what `feedback-status.mjs` actually reads. Result: the gate stayed red and 10 owner comments sat untriaged behind a handoff that said all were fixed. A check-off is not a fold; both files must be written.
- 2026-08-02 — owner (best-ai-video-generator storyboard, c20 + c53): **a rule folded into one surface leaves the identical defect on the other.** The 2026-08-01 fold wrote the per-item slot rule into `zone-rules.mjs` only. That fixed the conclusion (5/5 on `section/tool-intro`) and never touched the body, where `section/section-card-flip` opened 4 of 5 tools AND divided the Pricing section. `R_STRUCTURAL` was satisfied the whole time: section-card-flip IS structural, so "use the same structural card" held while the slot was both incomplete and shared between two meanings. Sameness is not completeness, and it is not exclusivity. `R_STRUCTURAL` gained cover-every-item and one-card-one-meaning; `R_ZONE_STRUCTURAL` gained count-the-roster. Enforcement routed to plan 179, because prose has now failed this owner twice on the same complaint.
- 2026-08-02 — session (best-ai-video-generator): **"no legal window existed" was a rule artifact, not a script limit.** HeyGen had no section opener because "let's move on to HeyGen" lands at 997s, inside the Synthesia screen recording, and E5 forbids any fullframe card over demo footage. But a section BOUNDARY is by definition the moment nothing is being demonstrated, so the rule was protecting nothing and deleting a card. E5 now exempts cards whose catalog `roles` include `section-opener`, bounded to 8s and to `demo` only (never `playback`, which is the proof shot). Lesson: when a rule makes a required outcome unreachable, check whether the rule's own justification actually applies at that moment.
- 2026-08-02 — owner (c01): **a gate can carry the right constant and still never look at the defect.** `HERO_RATIO_MAX = 4` was added 2026-07-31 from "heading very big, rest all very small", but it only measures font sizes behind a selector matching `\.(?:row|item)\b`. `tool-icon/tool-glass-tile`'s descriptor is `#subtitle`, so a 150px-over-32px cliff (4.69x) sailed through, as does the 36px row floor. A sweep found 17 of 55 fullframe cards over 4x invisible to the gate — though most are legitimate eyebrows, which is exactly why a selector-name heuristic cannot decide this. Fix is a declared `--body-size`, mirroring `--hero-size`; routed to plan 178.
- 2026-08-02 — owner (c27): **card-qa has never photographed a card's tightest layout.** `enacted/pipeline-flow` variant-b hardcoded `.chain { height: 760px }` whatever the step count, so flex connectors absorbed 472px of slack at 3 steps (two ~236px bare lines) and 150px title + 64px margin + 760px chain overflowed the 840px padding box by 134px, disabling the frame's centering. `card-qa.mjs` builds a `max` variable set but `generateVariables` never emits a `variant`, so every sheet is shot on `variants[0]`. Heights now derive from step count; card-qa variant coverage routed to plan 178. Lesson: "the max variant is clean" means nothing if `max` only ever meant max ITEMS, never max LAYOUT.
- 2026-08-02 — session (best-ai-video-generator): **a card can be built, approved, lint-clean, rendered, catalogued and still not be in the video.** `enacted/promise-shelf` was commissioned from owner feedback, had its look approved from a Gemini preview, passed every card gate and had a render sitting in `renders/`, yet `cues.json` had z01 back on the old `slate/kinetic-sentence` — a helper script from an earlier round had rewritten the zone block and reverted it. Nothing failed; the orphaned render was the only trace. Found by diffing render filenames against `resolved.json`. Worth a gate: any render file whose `<id>-<card>` no longer matches the resolved cue is either a stale orphan or a silently reverted cue, and today there are 20 of them in one workdir.
- 2026-08-02 — session: **the rulebook gates are per-surface and one of them is easy to forget.** Editing `zone-rules.mjs` without running `build-zone-prompt.mjs` left the zone prompt stale; `check-rulebook.mjs` said "rulebook ok" because it only checks the cue surface. The failure came from `scripts/check.sh`, which runs all three. Never trust a single rulebook check after touching a rule file.

- 2026-08-02 — owner (z01): **a still preview approves a LOOK, and only a look.** `enacted/promise-shelf` was approved on 2026-08-01 from a Gemini still and built exactly to it. Seeing it MOVE, the owner rejected the two things a still cannot show: the depth-of-field focus pull ("all gets blury") and the empty plinths ("just icons and no info, no actual content"). Nobody was wrong at any step — the approval never covered what was being approved. -> `steps/037-approve-card-plan-human/README.md`: a card whose device is motion, or whose defining property is absent content, is not approved until someone watches it render.
- 2026-08-02 — owner (z01): **never express focus with blur.** Defocusing the non-subjects means most of the frame is soft for the whole shot, and the viewer reads a broken render rather than an emphasis. promise-shelf now dims and lifts instead — same walk across the row, nothing ever out of focus. -> `card-library/DESIGN.md` (Motion).
- 2026-08-02 — owner (c01#2): **a ratio rule gives you a BAND; land in the middle of it.** The first pass moved the subtitle 32px -> 40px, which is 3.75x on a 150px hero: legal, but one notch off the FLOOR of the 2.5x-4x band. The owner came straight back with "this still looks small". Now 52px (2.88x). The edge of a band passes the checker and gets rejected by the person, costing a whole review round on a number already touched. -> `card-library/DESIGN.md` (Typography).
- 2026-08-02 — session: **a plan's test_cmd must run in every directory the plan writes to, and plan 177 proved it again from the authoring side.** 177 landed `beat_source: "transcript"` into `card-library/catalog.json` but never taught `card-library/scripts/check-catalog.mjs` the new value, so `tool-icon/roster-pop` sat on main failing `check-cards.sh` while the plan's own gate stayed green — its test_cmd ran only inside `visuals-flow`. The plan's own `needs:` line named the catalog as a shared target, so the information was there and the gate still did not cover it. Same shape as LESSONS 2026-07-21, but authored in, not executed in.
- 2026-08-02 — session (z01): **a full-bleed layer stops being full-bleed the moment you pan it.** The frame gate (E12, plan 175) exempts elements covering all four edges, which is correct. promise-shelf's `#key` was `inset: 0` and the timeline panned it 22px, uncovering one edge and voiding the exemption on every render. Fixed by bleeding it to `inset: -40px` rather than by loosening the gate — a panning background must be bigger than its travel.
- 2026-08-02 — session (z02): **a lead can push a cue out of its own register span.** z02's anchor IS the first word of the light span, so any `lead > 0` started the card inside the preceding dark span and tripped E8. Set lead to 0. Worth remembering when a register boundary and an anchor coincide: lead moves the card, not the boundary.

- 2026-08-07 — owner (c04/c16/c24): **a card that animates copy must be paced by the SPEECH, not by the clip.** `prompt/prompt-typing` spread its text evenly across `data-duration`, which comes from `catalog.default_duration` — a per-card motion constant with no relation to how long the presenter takes to dictate. Measured: 5.3-6.8s of speech typed over 10.95s, so 30-44% of the prompt was still missing when the voice had finished saying all of it. The owner asked the right question ("is this a storyboard artifact or real?") and the answer was real; `render.mjs` rewrites duration to the same number. -> new `lib/spoken-span.mjs` measures the copy against the transcript, catalog `spoken_var` opts a card in, the card takes `typeSeconds` and holds the finished text for the remainder.
- 2026-08-07 — owner (c09): **a static accent on a card that reveals over time reads as a stuck card.** `process/step-flow` painted step 1 orange with `.node-wrapper:first-child .label`, nothing to do with beats. Once steps 2 and 3 arrived the card looked like it had stopped following the voice. -> the accent now cross-fades onto the current step and hands off. Implemented as two stacked label layers rather than a colour tween, because `check-type-scale` reads the stylesheet and a GSAP-only accent leaves no `color: var(--accent)` rule for it to find — worth knowing before "moving a colour into the timeline" anywhere else.
- 2026-08-07 — owner (c10): **a "prefer" in prose is a defect generator until it becomes a lint** (the same shape as 2026-07-31). The STATEMENT TEST had said since 2026-07-31 that a description of on-screen action is never a sentence-card job, but the only machine check was E11's navigational-phrase regex, so the rule reached the authoring model as advice and nothing verified it. c10 shipped "it is automatically in here already" — a fullframe card replacing the very screen "here" pointed at. -> E11 `no-deictic-slate`, plus the clause in `cue-rules.mjs` and a rebuilt prompt. Boundary that mattered: "It's all here, and it works" (z02) is a standalone claim and stays legal, so the rule bars multi-word POINTERS, not the word "here".
- 2026-08-07 — owner (c28): **a beat that MIRRORS the words belongs on the first word; a beat that CONCLUDES from them belongs on the last.** Every beat resolved to `m.start`, which is right for step-flow's "Open the Train section" and wrong for a verdict chip: "Facial features hold" was readable 3.6s before he finished saying "her facial features do not change" (a phrase with a 1.9s pause inside it). -> catalog `beat_align: 'start'|'end'`, default `start`, opted in for `overlay/verdict-chips`.
- 2026-08-07 — session (c28, found by looking at the frame): **an unconstrained string that the card branches on is a silent inversion waiting to happen.** `overlay/verdict-chips` tests `verdict === 'yes'`, the catalog declared `verdict` as `role: free` with NO enum, and this video's cues said `pro`. All three positive chips rendered as RED CROSSES — the graphic asserted the opposite of the narration, and every gate was green. Nothing but a rendered frame could have caught it. -> enum added (validator already enforces `beat_shape` enums, so this was preventable), cues corrected, card made to accept both vocabularies. Swept the library: only two cards branch on a beat field and both now have matching enums.
- 2026-08-07 — session: **the board preview was not required to match the render, and only matched by luck.** `render.mjs` rewrites `data-duration` to the resolved duration; `serveCard` did not, so a card whose motion is paced by `data-duration` animated at one speed on the storyboard and another in the cut, from one plan. Three of 68 cards read `dataset.duration` for motion; `prompt-typing`'s hardcoded 12 happened to equal its resolved 12, which is the only reason the storyboard looked honest while the owner was asking whether he could trust it. -> one line in `board.mjs` plus a regression test pinned to a fixture where the two numbers differ (6 vs 8).
- 2026-08-07 — owner (c20): **a vector stand-in is a placeholder, not a design choice.** `enacted/character-card-stamp` argues that one face survives every scenario, and it was arguing it with a flat drawn head because nothing supplied `anchorImage`. -> real photorealistic portrait from the presenter's own dictated prompt, seed pinned so a re-render returns the identical face (on a card whose argument IS sameness, an unpinned seed destroys the point), inlined by the new `lib/images-inline.mjs` on BOTH surfaces. Rule written into `card-library/DESIGN.md`. A raw relative path would have resolved differently on the board and in the renderer, which is why this needed a data-URI inliner rather than a path.

- 2026-08-07 — owner (c01/c12/c29): **a hero FLOOR with no ceiling on the text is half a type rule.** `section-card-flip` paired a fixed 160px hero with a 700px title box, which fits about eight characters per line, so "Create Your Base Character" set one word per line and pushed the last word past the plate. `check-type-scale` enforces a 120px minimum and nothing anywhere checks the text FITS; the overflow probe exists but is deliberately not a blocking gate. The card's own demo title is "The Winners", short enough that the gallery never showed the defect, which is the general trap: **a card's default variables are the one case that gets looked at, so they are the one case that must NOT be the easy one.** -> the card measures and shrinks its hero to the floor until the title takes at most two lines, the box widened to 1180px (the plate shrink-wraps, so 700 was narrow for no reason), and the catalog gained `max_chars: 26`.
- 2026-08-07 — owner (c44): **a rule implemented in half is a rule that generates the defect it forbids.** W22's text said "number every major section 1..N **or** drop the number variable", and the code only checked that the numbers present were consecutive. A clean 01..07 run followed by one unnumbered card passed silently and read to the owner as a missing number, which is the same complaint W22 was written for in the first place ("why section 4 suddenly"). -> W22 now also warns when any section card is numbered and another is not. When writing a lint from a rule with an "or" in it, implement both branches or the gate is decorative.
- 2026-08-07 — owner (c15): **a symmetry lint that needs two members cannot see a series of one, which is the worst case.** E14 requires `concept.throughline.items` (this video's throughline is name/description/evolution, so E14 never ran at all) AND two or more uses of a card before it inspects them. A single `overlay/step-card` reading "Step 3 of 7" therefore shipped against symmetry feedback the owner had already given several times. The card DECLARES its own denominator, so no roster was ever needed to catch it. -> new lint E16 series-incomplete: a card carrying `step`/`total` must have all N members. Cue deleted rather than completed, because the section cards already number the same seven steps.
- 2026-08-07 — owner (c04#2): **fixing the rate is not fixing the exposure.** Round 1 made prompt-typing type at the speaking rate but left duration on `default_duration`, so the card sat holding a completed prompt for 5.3s of a 12s clip and the owner came back with "when she says influencer, you can stop there". A `spoken_var` card now ends when the dictation ends plus a 1.0s read tail, and is exempt from the sentence-boundary extension, which would otherwise have restored the hold (c04's next boundary is 8s out, which is how it reached 12s originally). Two rounds on one card because the first fix addressed the symptom the owner named rather than everything the mechanism controlled.
- 2026-08-07 — owner (c20#2): **a card build may not narrow the concept and then write the narrowing down as its contract.** `concept.json` said the character card gets "duplicated into kitchen, travel and product frames that all carry an identical face". Step 240 built one image reused in every frame with a per-frame colour tint, and enshrined it in the card header as "the point is sameness, so every frame renders the SAME image source; nothing in this card may vary the portrait". The owner saw it immediately: "you are just adding an effect on the same image, it's not the person in a different scenario." The face is the constant, the scenario is the variable, and the build inverted which was which. -> per-beat `image`, tint demoted to a whisper when a beat carries its own picture, and the frames sourced from the video's own demo footage so the face is identical because the tool made it so. **When a card is built, diff its header contract against concept.json before shipping.**
- 2026-08-07 — owner (z02): **a whitelist written into a fresh rule is the next defect.** Writing E11 in round 1 I explicitly exempted "It's all here, and it works" and added a test asserting it stay legal. The owner rejected that exact line in round 2 and pointed out the objection applies to `keyword-statement` as well as `kinetic-sentence`. -> "here"/"there" are now barred outright in any sentence card, the rule text names both cards, and the old test is inverted rather than deleted. The general lesson: when a fold hands you one rejected example, do not spend the same fold carving out the nearest sibling; the owner's objection is usually the class, not the instance.

- 2026-08-07 — session (verification pass, round 3): **a catalog flag that changes a SHARED resolver is a change to every video, and the fold that adds it only ever checks one.** Adding `beat_align: 'end'` to `overlay/verdict-chips` fixed c28 on consistent-ai-influencer and silently created an E9 overlay-over-graphic error in TWO other videos: end-alignment moves a card later by the length of its first anchor phrase, so its TAIL moves too, and best-ai-video-generator c19 gained a 0.90s overhang into c20 while opusclip-vs-submagic c18 gained 0.50s into c19. Both videos had zero errors before. Nothing would have caught it until someone re-resolved them, months later, with no memory of why. -> the resolver now clamps an overlay's tail so it cannot cover a following fullframe (it already knew that layout was illegal; it was reporting it instead of avoiding it), and only leaves the overlap for E9 when trimming would cut a reveal that has not fired. **Rule for future folds: after changing anything in `resolve.mjs` or a shared catalog field, re-lint every video, not just the one in the fold.** A read-only probe that resolves in memory and diffs against each stored `resolved.json` is the cheap way; `run.sh resolve` on another video REWRITES its artifacts, which is its own hazard (done accidentally during this very audit and reverted).
- 2026-08-07 — session (verification pass, round 3): **never `git checkout --` a file the fold has been editing.** Reverting `card-library/catalog.json` to undo a two-line probe wiped all six uncommitted catalog changes from both rounds (`spoken_var`, `typeSeconds`, `beat_align`, the verdict enum, the section `max_chars`, the per-beat `image`). Recovered by reapplying from the fold notes, then re-running every gate, but only because the notes were detailed enough to reconstruct from. Probe a shared file with an in-memory copy, or write the probe to a temp path; the working tree of an uncommitted fold is the one place a revert is unrecoverable.

## Convergence

- 2026-08-07 consistent-ai-influencer (verification pass, round 3): 17 items audited (9 from round 1, 8 from round 2), all 17 marked in BOTH `feedback.json` and `claude_status.json`, 0 lint errors on the video, 755 tests green. Found 1 latent cross-video regression introduced by round 1 (the `beat_align` tail, above) and 2 documentation gaps: `spoken_var` and `beat_align` existed as live catalog fields with no validation and no mention in any doc a card author reads, so the next card to need either would have repeated c04 or c28. Closed with `check-catalog.mjs` validation plus a DESIGN.md new-card entry for both. Net durability after the pass: 8 of 17 items are gate-backed (a lint or validator rejects a recurrence), 6 are mechanism-backed (automatic given the catalog flag, and the flag is now validated and documented), 3 need no guard (2 box tests, 1 instance renumber).
- 2026-08-07 consistent-ai-influencer (storyboard review, round 2): llm=55 approved=57 edited=2 added=5 removed=3 typed=8 chat=0 flags=0 lint-errors=0. 8 board items -> 6 durable changes (`section-card-flip` + `character-card-stamp` + catalog, W22 widened, E16 added, E11 widened, `resolve.mjs` exposure, plus a `max_reveal_chars` fix the work surfaced), 2 cue deletions, 1 renumber. **Three of the eight were repeat rounds on round 1's own items** (c04#2 exposure vs rate, c20#2 tint vs scenario, z02 vs the whitelist I wrote), which is the number to watch: round 1 closed 7 items and re-opened 3. All three had the same shape, a fix aimed at the symptom the owner named instead of the whole mechanism behind it. The other five were latent defects with rules already written and gates that could not see them. `typed` went 7 -> 8 rather than falling, but 5 of the 8 are first-sighting defects in cards this video is the first to stress.
- 2026-08-07 consistent-ai-influencer (storyboard review, round 1): llm=55 approved=59 edited=1 added=5 removed=1 typed=7 chat=0 flags=0 lint-errors=0 lint-warnings=27. 7 board items (9 including two feedback-box tests) -> 6 durable changes across 5 surfaces (card-library card x3, catalog, `lib/resolve.mjs`, `lib/lint-cues.mjs`, `lib/board.mjs`, `DESIGN.md`), 2 instance fixes, 1 architectural item deferred by the owner. Four of the seven items shared one root cause (typing paced by the clip, not the speech), which is the reason to root-cause before fixing rather than after. Two defects nobody reported were found while answering the owner's questions: the board/render duration divergence, and verdict chips rendering pros as red crosses. The second only surfaced because the fold extracted frames instead of trusting a green gate — `edited=1` understates the round because the fixes landed in rule surfaces rather than in cues.
- 2026-08-02 best-ai-video-generator (round 3, final-cut review v6->v7): llm=86 approved=84 edited=19 added=0 removed=2 typed=17 flags=0 lint-errors=0 lint-warnings=18. The edited/added/removed figures remain UNUSABLE as a convergence signal for this video: `cues.llm.json` was re-copied after every fix round during the original run, so the 19 "edits" are mostly this session's own folds (10 of them are the five tool-intro conversions across two rounds), not owner-vs-LLM divergence. Reported rather than omitted so the zero is not mistaken for agreement.
- 2026-08-02 best-ai-video-generator (round 2, storyboard review): llm=88 approved=84 typed=6 folded=5 deferred=1 durable=4 instance=3 plans=3 lint-errors=0. `edited`/`added`/`removed` are NOT reported for this video and the number `edit-delta` prints is false: `cues.llm.json` was re-copied over the baseline after every fix round during the original run, so the LLM's first draft no longer exists. Nothing can be inferred from this row about how well the cue pass did. The process fix is simple and not yet made: snapshot `cues.llm.json` once, at the end of 035, and never write it again. Until that lands every convergence row from this video is unusable.
- 2026-07-25 test-03 (round 1, first video through the visuals-flow-feedback skill): llm=13 approved=13 edited=2 added=0 removed=0 typed=6 flags=0 lint-warnings=5 (6 typed items → 6 durable rule/card changes and 0 instance-only, the best ratio so far; 2 edits are the two cues re-pointed at the new cards. Two NEW cards were built from the feedback — `enacted/promise-split`, `link-in-description/link-scrim` — after the owner pointed out that sessions never propose templates and only reuse existing ones.)
- 2026-07-25 test-01 (round 2): llm=23 approved=23 edited=16 added=0 removed=0 typed=30 flags=0 lint-warnings=6 (Final Cut review round; 30 typed items is the whole v1+v2 comment backlog folded in one pass — 12 became durable rules, the rest were instance fixes already shipped in v2..v4. `edited=16` is high because v1's card deletions forced remaps.)
- 2026-07-18 test-01: llm=27 approved=18 edited=11 added=1 removed=10 typed=7 flags=0 lint-warnings=7 (v2 baseline; heavy edits expected — three new rules folded this pass)
- 2026-07-18 test-01 SHOTS: llm=9 approved=9 edited=0 added=0 removed=0 typed=0 flags=0 lint-warnings=0 (v1 shot-pass baseline — perfect convergence on the first try; watch whether it holds on video #2)
- 2026-07-18 — owner: typed board feedback silently lost on reload without Save (c26 "too plain" note) → board warns on unload with unsaved feedback boxes; lost note re-entered from screenshot and processed.
- 2026-07-18 — owner: full-color brand logos clash with the warm palette → DESIGN.md "Tool logos" rule: always muted (saturate .5, brightness .95, opacity .9), small relative to text; applied across all 6 logo-consuming cards.
- 2026-07-18 — owner: persona-match "too plain" for the finale → card rebuilt (THE VERDICT eyebrow, panel rows, drawn accent dividers, gold winner chips + one-shot glow); gold-chip winner pattern recorded in DESIGN.md.
- 2026-07-18 — session: Approve then Save silently reset approved (raw JSON.stringify cue comparison is key-order sensitive; script-written cues.json ordered differently than the board serializer) → canonicalized comparison in handleSave.

- 2026-07-20 — owner (test-02 board review): comparison cold-open must feature the tools, not bury them in a subtitle → c01 switched to title-aurora-wave with 5 logo chips; RULEBOOK+prompt cold-open rule; aurora-wave gained a `platforms` variable (catalog + card build + logos-inline `platforms[].logo` walk).
- 2026-07-20 — owner (test-02): bar-chart bars rendered empty + bare unitless numbers → root cause: cue passed no per-bar `color` and `linear-gradient(undefined)` is invalid CSS (invisible fill); fixed with a card fallback palette + `$` prefixes; RULEBOOK+prompt "units mandatory on numeric cards"; catalog documents prefix/suffix.
- 2026-07-20 — owner (test-02): table-rows sat as an empty bordered box for 18.9s before the first row + no logos → lint W5 first-beat-idle (max 8s, re-anchored c29 to 4.0s); table-rows panel now enters WITH the header, and `beat_shape.logo` renders a muted per-row logo; RULEBOOK+prompt "beat cards must not idle empty".
- 2026-07-20 — owner (test-02): verdict-trophy showed a mystery Hostinger icon + joined two winners in one box → the card's demo-default logoSvg was Hostinger's and always rendered; now an unset logo HIDES the box and a registry `logo` slug is supported; c32 split into two back-to-back trophies (n8n, Flowise); RULEBOOK+prompt "one winner per verdict card".
- 2026-07-20 — session (test-02): Groq whisper-turbo emitted 61/5543 words with tiny (≤0.52s) non-monotonic starts; the GFX-04 validator REJECTED the whole transcript → forced garbled local-whisper fallback + burned an hour of Groq audio quota per retry. Fixed: transcribe-groq.mjs clamps sub-1s backwards starts, rejects only if >2% of words are non-monotonic. Also folded: 010 README now mandates transcript brand-QA (normalize + merge split-word garbles) BEFORE the cue pass, since 090 burns captions verbatim.
- 2026-07-20 — session (test-02): cue-pass logo guidance hard-coded test-01's 5 seeded slugs → prompt+SKILL now inject the live registry slug list ({{LOGO_SLUGS}}) and tell the session to fetch missing logos first.

## Convergence (cont.)

- 2026-07-20 test-02: llm=32 approved=(pending) edited=6 added=1 removed=0 typed=4 flags=0 lint-warnings=17 (W1 cadence×15 + W2 overlay-density×1, all script-structural; first real video on the Youri grammar + first bar-chart/table-rows/aurora-wave/dual-trophy exercise — 4 typed items ALL became durable card/lint/rule fixes, 0 instance-only)

- 2026-07-20 (r2) — owner (chat): title white logo boxes "look terrible" → aurora-wave chips reworked to dark GLASS (design-system panel style) with muted logos + faint inner plate; root-caused a second issue where opaque-white logo JPEGs (Make) broke the dark look → logos-inline.mjs now inlines transparent .svg logos (Make swapped to a brand-purple simple-icons SVG); frame-verified 5-chip title.
- 2026-07-20 (r2) — owner (chat): merge the two winner trophies into one side-by-side graphic, scalable to more winners without quality loss → NEW card verdict/winners-podium: 2-4 tool columns revealing one per beat over confetti, layout (col width/font/logo/gap) derived from winner count so 2 reads premium and 4 stays clean; `ranked:false` = co-winners all gold, `ranked:true` = gold/silver/bronze/steel medals. Frame-verified at 2 (co-winner) / 3 / 4 (ranked). beat-smoke card counts 14→15 beat, 46→47 total.

- 2026-07-20 (r3) — owner (board): Make logo broke in the BOARD (broken-image box) though it rendered in the mp4 → root cause: SVG data-URI renders in hyperframes' chromium but not reliably in a browser <img>. Fixed by rasterizing the tinted Make SVG to a TRANSPARENT PNG (rsvg-convert) — PNG data-URIs work in board + render. logos-inline SVG support kept (harmless) but Make now ships PNG.
- 2026-07-20 (r3) — owner (board): winners-podium showed only n8n + timing off → the two co-winner beats were anchored ~6s apart, past the card's in-window reveal, so the board preview (card's own 8s) never showed Flowise. Re-anchored both to the naming sentence (~2s/~4s). Also: confetti now wraps within [-110,1190] (no overflow badge) and a 20s timeline spacer keeps it alive through long holds instead of freezing.
- 2026-07-20 (r3) — owner (chat): disliked the intro; wants title-cinematic-float style with all tool rectangles the SAME size → cinematic-float given the glass-tile + registry-logo treatment, every tile identical (210x210) with the logo at a fixed 58% centered footprint so brand marks of different fill/aspect read uniformly; c01 switched from aurora-wave to cinematic-float.
- 2026-07-20 (r3b) — owner (board): intro tiles showed TEXT names not logos + podium re-flagged overflow + previewed 0:07 not 18s → root cause: the board is a long-running process; card HTML is read per-request but the enrichLogos MODULE (which gained platforms[].logo walking) and the VO slice cache were stale from a pre-edit start. Fix: restart the board (re-imports logos-inline). Verified via curl that /card/c01 now injects 5 logo data-URIs and the c32 slice regenerated to 18.14s. Also hardened podium confetti to spawn/sway/wrap STRICTLY inside 1920x1080 (was overflowing horizontally on sway + vertically on the negative start) so the overflow probe stops flagging it. LESSON: after editing any lib/*.mjs that the board imports, RESTART the board before review.
- 2026-07-20 (r3c) — owner: show the tool NAME with the logo on the intro tiles, kept symmetrical/no-overflow/generic → cinematic-float tile restructured to a fixed logo box (86px cap, uniform footprint) above a name label (nowrap + ellipsis guard); tiles now 196x218, identical, wrap for any count, name from platforms[].name. Frame-verified (LangChain, the longest, fits). aurora-wave left logo-only (its light-bar reveal math is tuned to its chip width; not in use for test-02). Confirmed for the owner: card templates are LOCAL video assets (card-library → hyperframes → kb-scratch mp4 → final video); they do NOT publish to agrollo or any site.
- 2026-07-20 (r4) — session (test-02 draft watch): burned captions rendered literal "0,0,0,," before every line → assemble.mjs [Events] Format declared only "Layer, Start, End, Style, Text" (5 fields) while each Dialogue line writes the full 10-field ASS row (…,Cap,,0,0,0,,text); libass swallowed the margin/effect fields into visible Text. Fixed Format to the standard "Layer, Start, End, Style, Name, MarginL, MarginR, MarginV, Effect, Text". Verified with a standalone libass render (clean text). This shipped in plan 102's libass caption pipeline and was never caught because no draft was frame-verified WITH captions on until now.

- 2026-07-28 test-03 (final-v3:0) — owner (board): "add cta for link in description whenever mentioned; first mention link-scrim, rest the green card; if the green card is not fixed pls fix it" → THREE root causes, one item. (1) No rule tied a description mention to a CTA: the v2 fold placed one scrim cue BY HAND and nothing generalized it, so of three mentions in test-03 the second got no card and the third sits in an uncut conclusion → NEW R_LINK_CTA (every mention gets a CTA; first=link-scrim, later=pill; count across the whole script). (2) The "green card" was never fixed, only un-broken: overlays already render .mov as ProRes yuva444p12le — a REAL alpha channel — so the chroma plate was painted into a format that never needed it and keyed back out imperfectly, leaving 14.0% green spill in the pill's drop shadow. Re-authored transparent: spill 14.0% → 0.0%, measured on a composite over real footage. (3) Found while checking it: like-subscribe painted the same #00b140 plate while its catalog entry declared NO `chroma`, so nothing ever keyed it and it composited as a SOLID GREEN RECTANGLE over the whole frame — the exact card the conclusion's subscribe CTA needs. Both cards transparent, `chroma` removed from catalog (it was the only user), which also retires the FCPXML chroma-export gap plan 154 was written for. LESSON: "the key works" is not the same as "the card is right" — check whether the key is needed at all before tuning it.

- 2026-07-30 opusclip-vs-submagic (pre-render fold) — owner (chat + 037 board): FOUR items, three durable. (1) "the new motion graphic template you build was not intuitive, it was complicated to follow" → `enacted/same-input-split` was built, passed `hyperframes lint`, `card-qa`, `check-type-scale`, `check-catalog` and `check-cards`, and was still unreadable: it stacked four visual systems (source tile, two rails, two travelling clones, chip row) for ~9s of screen time. Root cause is a MISSING gate, not a broken one — the mute test exists at 050 but audits the cue PLAN against catalog `purpose` strings and never sees a built card's pixels, so "can a viewer follow this in one viewing, muted?" was asked of the plan and never of the card. Card deleted, slot re-cued to `enacted/pipeline-flow`; DESIGN.md new-card checklist gains "legible on one viewing, answered FROM A RENDERED FRAME". Routed to DESIGN.md and NOT the zone rulebook despite the `zone-intro:*` key, because the lesson is about how a card is BUILT and the body pass proposes cards too — the key records where the card was used, not what the lesson is about. (2) Owner then corrected the fold itself: the session had recorded that rejection as two invented general rules, one of which ("prefer reusing over commissioning") directly CONTRADICTED `R_ZONE_SHARED_CATALOG`'s "commissioning a new card is an expected outcome, not a last resort", and would have been folded into the same rulebook that says the opposite → 130 README now requires a folded lesson to quote the owner verbatim, scope to the evidence (one rejection is evidence about one artifact), and be contradiction-checked against the target surface before it lands. (3) "i prefer if every motion graphics has some color logo, image etc.. Not just text" → survey found 39 of 52 fullframe cards text-only, only 13 accepting a logo slug; the card that raised it (`enacted/spotlight-focus`) named OpusClip and Submagic as bare words. `spotlight-focus` and `enacted/race-bars` now take `productLogos[]` — index-aligned, and deliberately a shape `lib/logos-inline.mjs` ALREADY inlines, so no library change and no silent letter-fallback. Frame-verified through the render path. DESIGN.md gains "A graphic carries a mark, not text alone"; the remaining 37 are recorded debt, and no NEW card may be added text-only. (4) "not able to play video" (0:00/0:00 on the per-cue player) → NOT a player bug: the board builds per-cue slices at boot only when `resolved.json` is present, and Gate 1 at 037 must be reviewed BEFORE 040 exists, so it had booted in card-plan-only mode. Restart produced all 39 slices. LESSON: the same shape as r3b two months earlier — the board is a long-running process started against an earlier state; restart it before believing what it shows.
- 2026-07-30 Convergence (opusclip-vs-submagic) — 39 cues from LLM, 39 approved, 2 edited, 0 added, 0 removed. **The `edited` count is not trustworthy for this video**: `cues.llm.json` was re-copied after every fix, so 11 anchor rewrites, 13 register additions, a dropped cue, an added section card and a card swap all measured as zero. Fixed at the tooling level — `lib/edit-delta.mjs` now warns when the baseline is byte-identical to `cues.json` instead of reporting a clean zero, with a regression test. Snapshot once, when the pass converges, and never again.
- 2026-07-30 opusclip-vs-submagic (pre-render, item 5) — owner (chat): "pls dont make link in descirption, subscribe related teampltes - text dynamic. lets keep it constant and simple" → the three CTA cards each took an authored text variable (`message`, `kicker`, `subscribeText`) for an ask that is identical in every video, and this video had already drifted: c05 said "Both tools linked in the description" while z13 said "Link in description" for the same CTA. Copy baked into all three cards; all three catalog entries now declare `variables: {}` so the resolver REJECTS authored copy rather than a rulebook sentence asking nicely (the routing table's "machine-enforced surfaces beat prose"). `link-scrim`'s `kicker` was a second authored line — element, CSS, render lines and tween all removed, not just defaulted to empty. The `--variables` override comments in each card header were corrected; they had become instructions to do the thing the card no longer supports. R_LINK_CTA gained "CTA cards carry NO variables" and `cue-pass-prompt.md` was regenerated, because a rule not in the generated prompt never reaches the model. Frame-verified: scrim and subscribe render their constant copy; the pill confirmed through the board's /card endpoint (render refuses pre-Gate-2, correctly).
- 2026-07-30 opusclip-vs-submagic (pre-render, item 6) — owner (board): "why was this overflow issue not fixed. this card looks bad. more than fix - i want rca why this was not auotmaltically fixed" → c17 `enacted/pipeline-flow` clipped its title top AND its last node off the canvas, reproduced at true 1920x1080 (the board preview is a real 1920x1080 iframe at `transform:scale(0.25)`, so the badge was trustworthy, not a preview artifact). THREE gaps, all required. (1) The overflow probe is a BADGE, NOT A GATE: it lives only in board.mjs as iframe-side JS posting `__overflow` to paint a red chip; grep found zero consumers, so the pipeline detects clipping and ships it. (2) `card-qa` tests the CARD, never the CUE — it renders declared min/max with synthetic content and never sees the real title, so "no clipping at max" certified a card that overflows on actual copy. (3) `resolve.mjs:361` rotates `variant` by use count AFTER the copy is authored: pipeline-flow used 3x put c17 alone on variant `b` (vertical chain, least vertical headroom) while z05/c19 stayed horizontal, and `title` had NO `max_words`, so a 4-word title that fits `a` wrapped to two lines in `b`. FIX: title capped at `max_words: 3` = variant B's real limit, using the existing `validateVariable` machinery so it fails at 040 rather than rendering clipped — proven by the cap rejecting all three titles before they were shortened. DESIGN.md gains "Declared capacity is the TIGHTEST variant's capacity", because the resolver picks the layout after the copy is written. LESSON: a card with `variants` must declare the capacity of its worst variant, and a detector that only paints a badge is not a gate.
- 2026-07-30 opusclip-vs-submagic (plan 168, finished by hand after boss parked it) — the crew's half landed real: `lib/overflow-measure.mjs` (one source for board + gate), `scripts/overflow-probe.mjs`, and a prove-it-can-fail test on the actual c17 input. It could not merge because `node --test` printed all oks and then HUNG FOREVER: `probeCardVariant` caches a headless browser at module scope and only `main()` closed it, so a test file importing the function leaked the handle. Fixed by exporting `closeBrowser()`, calling it from `main()`'s `.finally` (a mid-run throw leaked it too), and wiring `after(closeBrowser)` in the suite — 5/5 pass, exits in 13s. **The substantive finding is bigger than the bug**: the probe says 31 cards (37 card/variant combos of 70) overflow at their DECLARED capacity. Verified against ground truth before believing it — pipeline-flow with the real shipped content (3-word title, 3 steps) measures clean, the same card at declared max (6 beats) overflows with the same offenders the board reported for the frame that shipped clipped. So the caps really are too generous, as decisions.md 2026-07-17 predicted ("conservative estimates from layout math, unverified until video #1"). **Two plan defects, mine not the crew's**: Step 5 specced deriving only `max_words` on text fields, but the usual culprit is `max_beats` — shrinking a title to fix a beat-count overflow never converges, which is why derive reported a bogus "title capacity is below 1 word"; it now reports `UNATTRIBUTED` with the offending elements and writes nothing. And I claimed an exit-code bug that did not exist — I had read `tail`'s status through a pipe. LESSON: a plan that specs a derived value must also spec WHICH declared field owns the failure, or the derivation optimises the wrong knob.
- 2026-07-31 opusclip-vs-submagic (storyboard review fold, 7 board items + 1 chat) — owner: "why is Speed first not highlighted" → promise-payoff ignored authored beats (hardcoded cadence, winner-defaults-to-LAST got the only accent); card now consumes `variables.beats` and lights every resolved option in accent when no `winner` is set, and new lint E12 fails resolve when a beat reveal names an item the card never declared (z03's beats revealed labels matching nothing — a silent no-op until now). owner: "static screen 0:05–0:17, I thought we fixed this permanently" → the fix that existed (ambient breathe 1.012) is imperceptible over a 17s hold; DESIGN.md gains the long-hold tier (>8s holds need a VISIBLE loop), pipeline-flow carries the reference track-sheen, and W20 warns when a cue holds a card past 2× its designed duration. owner: "pink and orange don't go together, change wherever" → rose #fb7185 swept from 16 cards; the batch-approved gold-sweep+red-fail split was then REVISED BY THE OWNER live on the board ("connecting line orange only", "why is the last box red") → fills/sweeps stay accent orange in every register, the red fail-finale is gone (all nodes resolve orange+check), red survives only as static no-marks; gold back to winner-only. owner: "heading very big, rest all very small — make a rule" → the hero rules had a 2.5× MINIMUM and no ceiling/floor, so 150px-over-28px was compliant; check-type-scale now enforces hero ≤4× row text and rows ≥36px (caught 3 cards). owner: "orange boundary not complete" → 1px @ 0.42α hairline died in encode; keyline rule ≥2px @ ≥0.6α, link-scrim at 2.5px. owner: "wrong template for this big text, small letters look bad" + statement-placement discussion → R_KINETIC gains the STATEMENT TEST (pull-quote test; never instructions/on-screen actions), word cap 18→12 with verbatim-core-clause escape, kinetic-sentence sizes by bucket (≤8w→120px, 9–12→96px). owner: "intro card complicated, redo it" → NEW `statement/strike-list` (direction C: statement holds frame, icon items struck in red per beat, marker underline payoff); bad-clip-montage's unwired hardcoded caption + mid-glyph clip repaired.
- 2026-07-31 opusclip-vs-submagic — RENDERER BUG found by frame-verifying c20: in any `--variables` render, `color: var(--accent)` on the kinetic-sentence word spans renders WHITE — as a GSAP tween, as a static class rule, and as an inline style — while the browser and the board show all three correctly and `getComputedStyle` resolves the token fine inside the same render. Every kinetic-sentence render to date shipped white accent words and no gate could see it. Card-side fix: resolve the token via getComputedStyle at build time and set the accent as an INLINE LITERAL. This is the strongest argument yet for the headless per-cue frame gate (routed to a plan this fold): three code-level "fixes" looked correct everywhere except the rendered frame.
- 2026-07-31 Convergence (opusclip-vs-submagic, storyboard fold) — llm=39 approved=39 edited=12 added=0 removed=0 typed=8 flags=0 lint-errors=0 lint-warnings=~20 (7 board + 1 chat typed items → 6 durable rule/checker changes, 1 new card, 1 renderer bug fix, 2 instance-only; `edited` includes this fold's 4 re-authored cues plus 15 register fields backfilled to satisfy E8, which had never fired on this video before)
- 2026-07-31 opusclip-vs-submagic (round 2 opens) — owner: "why am I not able to add feedback to cards where I added feedback earlier" → feedback was ONE slot per cue and a folded item closed the box forever; review rounds now chain (`c20` → `c20#2` → …): folded rounds stay visible as history, the box binds to the first unfolded key, the server keeps folded items immutable and strips `#N` for cue context (feedbackRounds + 5 vitest cases). owner: "save button doesn't have any response… no idea whether my feedback got saved" → a clean save was SILENT and a network failure rejected silently; Save now shows unsaved-changes dot+chip, "Saving…", a "✓ Saved" flash, banners on clean saves, and catches fetch failures loudly. Also killed the false OVERFLOW badge class: full-bleed elements (parallax `#bg`, inset:-10%) are a bleed not a clip — the shared measure skips four-edge covers and drops the tell-nothing document-scroll fallback (this also protects plan 175's E13 gate from the same false positive).
- 2026-07-31 opusclip-vs-submagic (round 2, 5 board items) — owner: z01 "total out of sync… after so many checks" → true and self-inflicted: round 1 kept the old card's spread anchors, striking Captions BEFORE the word and Layouts 4.6s after; nothing validates that a beat's anchor names its reveal. Fixed + R_NO_IDLE now says a beat's anchor IS the naming phrase, and strike-list gained a marker-beat so the payoff word syncs too. owner: z05#2/c20#2 "still static, fix long term" → reconciled TWO standing owner rules that were in tension: "never cut mid-sentence" (v2:5) vs "don't squat on a static frame" — a fullframe card now ends at the FIRST sentence boundary after its own content instead of the LAST boundary before the next cue, and GAP_ABSORB dropped 12→4s (the 12s absorption was re-inflating finished cards; z05 17.2→6.3s, c20 11.1→6.3s, z11 15.2→10.4s). Comparison-family cards carry `hold_full` and keep the long hold — the VO reads from them. owner: c26 "why section 4 suddenly… I like section cards, don't scare the LLM off them" → numbered section cards are a SERIES: W22 warns on non-consecutive numbering, R_STRUCTURAL says section cards are WANTED and unnumbered mode always available; section-card-flip gained title-only mode; c26 unnumbered. owner: z14 → like-subscribe rebuilt as the full lockup (thumb pops orange, Subscribe press, bell rings+tints) in-theme, constant copy.
- 2026-07-31 opusclip-vs-submagic — BOARD DATA-LOSS BUG (second of its class): one owner Save wiped every `register` (27), `motif` (2) and `legacy_why` (1) from cues.json — the tile fragment deliberately shows only anchor/hold/variables/beats and collectCues rebuilt each cue from the fragment ALONE, deleting every field it doesn't display. Fix: tile models carry the original cue and collectCues spreads it under the fragment fields; regression test in board-ui/test/collector.test.ts. Related session lesson: `cat >` onto a test file CLOBBERED 9 tracked plan-172 guards because the filename looked new — restored from HEAD and appended instead. Check `git ls-files` before writing a "new" test file.
- 2026-07-31 Convergence (opusclip-vs-submagic, round 2) — llm=39 approved=39 edited=12 added=0 removed=0 typed=5 flags=0 lint-errors=0 (5 typed items → 4 durable rule/lint/card changes + 1 authoring-error fix; round-2 feedback flowed through the new `#N` round keys end to end; chat items this round: errors-only save banner, W20→W21 code collision)
- 2026-07-31 opusclip-vs-submagic (final-cut fold, 6 board items + 3 chat) — owner: "video should start with full screen avatar or motion graphic, make this a rule" → nothing governed t=0 (z01 anchored at 5.2s, s01 at 40s); resolve gains HEAD_COVER (first fullframe cue within 6s of open pulls to 0, its card-relative beats shift with it), lint E13 open-cover is the backstop, R_ZONE_OPEN_COVER teaches the zone pass. owner: "motion graphic went away before the concept finished… bring up the text when it's actually said" → z02 (promise-split) was a single-shot enact, both verdicts animated at entry; the exposure rule and the static rule can only both hold when card content is DISTRIBUTED across the narration → R_CONCEPT_PARTS (2+ spoken parts ⇒ word-anchored beat card), promise-split converted to a beat card (sides[2] + beats), and NEW `entry_phase` mechanism: an entry-phase card keeps its anchor placement (the anti-dead-air first-beat override was yanking z02 past the shared-promise sentence it exists to cover) capped at 8s before beat 1. owner: "buttons on screen before the blip — some sort of bug" → REAL BUG in the whip flash: the flash-in sampled the INCOMING card's own first frames as its blurred lead-in, so any card whose first frame shows full layout ghosted before its entrance; the flash now carries the OUTGOING footage through (fixed-length sources reuse their last half-second — slicing past a render's EOF produced an empty transition segment, caught by export-timeline tests). owner: "what was the point of that small amount of screen time" → SLIVER_GRAPHIC 2.5→6 / MIN_SCREEN_ERROR 2.5→6 (a 2.9s sliver escaped the old cap by 0.4s); graphic-adjacent slivers absorb into the card, between-avatars is now a hard plan error. owner: "avatar should start with a new sentence" → the rulebook only said "prefer"; lint E7 makes sentence starts/ends mandatory and W7 flags flush cuts (a legal sentence start with 0.00s silence before it still reads mid-sentence — s03's actual defect); s03 re-anchored to the AI-edit verdict wrap (268.5s, 0.46s breath), and E7 immediately caught s10 starting mid-sentence too (re-anchored to the comment CTA). owner (chat): "don't want bubble, i already clarified" → the corner-bubble baseline design is DEAD: assemble bubble default off, avatar verb --spans-only, 060 rulebook rewritten (host spans are the only avatar presence). owner (chat): like-subscribe "looks very bad" → hero-scale lockup (56px icons/42px text at 70% height with a 34-54px glow blob) rebuilt compact and flat (34px/26px at 78%, flat accent pill, no sheen, faint keyline pulse under a fixed shadow). owner: v1:5 (no graphics over the host) DEFERRED on owner instruction — feedback-status gains a `deferred` state so a parked item neither blocks the gate nor gets falsely marked folded.
- 2026-07-31 Convergence (opusclip-vs-submagic, final-cut round) — llm=39 approved=39 edited=12 added=0 removed=0 typed=6 flags=0 lint-errors=0 (6 board + 3 chat items → 7 durable rule/lint/mechanism changes, 1 transition bug fix, 1 card rebuild, 1 card converted to beats, 1 deferred; E7 found a second latent defect (s10) the moment it existed — the recurring shape: a "prefer" in prose is a defect generator until it becomes a lint)
- 2026-07-31 opusclip-vs-submagic (v3 fold, 2 board items, both instance-only) — owner: "Automating It Badly is not the main focus, suggest better" → the opening card framed the PAIN while the video's focus is the MATCHUP; owner picked the title/title-versus cold open (Submagic vs OpusClip, logos, no title line) from three options; strike-list stays in the library. owner: "remove this motion graphic for this video" → z06 (icon-pills criteria checklist) deleted, instance-only; the resulting 30.9s intro W15 gap is an owner call, recorded here so a later session doesn't "fix" it back.
- 2026-07-31 Convergence (opusclip-vs-submagic, v3 round) — llm=39 approved=38 edited=13 added=0 removed=1 typed=2 flags=0 lint-errors=0 (2 typed items, both instance-only, no rule changes — first round with zero durable changes; the rulebooks held)
- 2026-07-31 opusclip-vs-submagic (v4 fold, 1 board item — and an owner correction of the fold itself) — owner: "till 17 sec same static screen. why it passed your rule" → a REGRESSION from this session's own v1:3 fix: the 6s assembly hold-absorption silently froze z01's last frame over the resolver's DELIBERATE 5.8s screen return, and no lint could see it because absorption runs after all gates. First proposed fix (drop the cap, return to footage) was REJECTED by the owner as quality-degrading symptom-patching: "why can't we propose a better motion graphic… make those decisions correctly". Two durable lessons: (1) assembly absorption caps now sit BELOW GAP_ABSORB (2.5s glitch mop-up only) with a regression test — assembly can never override the resolver's plan; (2) a multi-second hole next to a card is a DESIGN task — the owner explicitly authorizes NEW intro/conclusion templates over template-reuse patching ("intro and conclusion are the most important part… make the best possible decision"). Result: commissioned `intro/versus-cold-open` (owner previewed the look via Gemini image prompts before the build): two-act enacted hook — clip strip drifts toward a closed POSTED slot while Captions/Pacing/Layouts stamp their cards in word-sync, then the strip sweeps and the Submagic-vs-OpusClip matchup lands and breathes to z02. New card type `intro/`, hero_shape none (first headline-less fullframe card), entry_phase, marker beat fires Act 2.
- 2026-07-31 opusclip-vs-submagic (v5 fold continued — the intro, third attempt, landed) — owner rejected versus-cold-open on sight ("starting entry itself is very weird": empty clip cards, dead top half, slot collision — the Gemini preview looked good because the generator invented rich card content the build never delivered) and rejected five more directions before picking SIDE MODE: host full-height right 720px, tape-board card left 1200px (marks + word-synced pain rows). New s00 side span (0-14.88, free), z02 re-anchored to its own sentence, new card `intro/tape-board-side` (side:true, entry_phase, hero_shape none). Side mode's FIRST production use found four latent defects in one afternoon, all of the gate-that-never-fired class: (1) E6 side-coverage read `c.slug` where resolved cues carry `c.card` — and its own tests used `slug` fixtures, so the gate passed tests while never working on real data; (2) avatar-render called lintShots without the catalog, so E6 rejected every side span as not-side-capable; (3) E7 rejected a span snapped to t=0 because the video edge wasn't in the sentence-start set; (4) --draft assembles at 1280x720 but panel/side geometry hardcoded the 1920 canvas — the host composited as an 80px sliver at the frame edge (a full-res final would have been correct; only drafts were wrong). LESSON: a mode's first production run IS its integration test — budget for it, and never let a lint's fixtures mirror the lint's own field names instead of real artifact shapes.
- 2026-07-31 opusclip-vs-submagic (v8 fold — the intro, fourth attempt: full-screen, and it stuck) — owner rejected the side-mode intro on the v8 review ("did not like, lets make a full screen motion graphic only") and approved the full-screen tape board from a preview frame with three riders: real logos in the tiles, an icon on every pain panel (logos/icons over bare text is a standing rule), and the pain panels visibly SMALLER than the app titles; plus "toning down the motion" (short travel, soft eases, no ambient float loops). Built `intro/tape-board` (fullframe, side:false), retired `intro/tape-board-side`, deleted side span s00. Two mechanism finds: (1) `avatar-jobs.json` is only rewritten inside avatar-render's --submit flow, so a span deleted from shots.json keeps compositing until its job entry is removed — v9 still drew the dead side host over the full-frame card; (2) the beat-smoke gate had been red-by-staleness since the catalog passed 48 cards: its hardcoded counts (15 beat/48 total vs 28/66 real) failed before its per-field checks ran, and those field checks predated beat_source "variables" (they demanded beat_shape/max_reveal_chars, which check-catalog FORBIDS on variables-sourced cards). Fix: counts updated, per-field rules delegated to check-catalog — one authority, no drift. LESSON: a gate that duplicates another gate's rules WILL contradict it eventually; delegate, don't recopy — and a count assertion that fails first can hide years of field-rule drift behind it.
- 2026-07-31 Convergence (opusclip-vs-submagic, v8/round-7 fold) — llm=38 approved=38 edited=13 added=0 removed=0 typed=1 flags=0 lint-errors=0 (1 chat item → 1 card rebuild via the preview gate, 1 span deletion, 2 mechanism fixes recorded above; the intro converged on its fourth built attempt and the owner approved the preview frame with riders instead of rejecting the direction — the preview gate is doing its job)
- 2026-08-01 opusclip-vs-submagic (Avatar IV finalize — a self-inflicted double-submit) — the owner ordered the III→IV swap; the engine work itself went cleanly (heygen-web --engine flag, engineMode production, enums learned from validation 400s), but the batch got submitted TWICE in parallel: a `node --test … | grep '# pass' && node avatar-render …` chain let the submit run even though the test failed, because the pipeline's exit status is grep's (it matched "# fail 1" happily), and that background task never "completed", so a second launch followed while the first was alive. Two writers interleaved on avatar-jobs.json (per-submit rewrites clobbered each other; a prepped video_id skip was lost) → 4 orphan Avatar IV renders ≈ 68s of metered pool wasted. LESSONS: (1) never gate a submit on a piped test run — `node --test` must be its own command whose OWN exit code gates the next step; (2) "one run at a time" needs a liveness check, not a belief: `pgrep -f avatar-render` before any submit launch; (3) avatar-render's per-submit rewrite drops jobs it hasn't reached and any extra fields — treat avatar-jobs.json as owned by a single running submit, never hand-edit it while one is alive.
- 2026-08-01 best-ai-video-generator (v3 fold, 9 board items) — the headline find is a RULEBOOK SELF-CONTRADICTION, not a model failure. The owner asked three separate times in one review why the video was not symmetrical ("I have said this multiple times and it again happened") and asked directly whether the cue LLM was broken. It was not: `R_STRUCTURAL` demands the same card for every parallel item while `R_REPETITION`/`CAP_FULLFRAME=3` caps non-structural cards at 3 uses, and this video compares FIVE tools. The evidence is exact — `tool-glass-tile` used 3 times (OpenArt/Higgsfield/Synthesia, none for HeyGen/Arcads), `spotlight-focus` used 3 times (OpenArt/Higgsfield/Arcads, none for Synthesia/HeyGen), while the cap-exempt `section-card-flip` stayed symmetric across 5. Both asymmetries land precisely at the cap. Fixes: R_STRUCTURAL now states THE SERIES ALWAYS BEATS THE CAP and that a per-item slot must be filled by a structural (cap-exempt) card, never by a capped general-purpose device; `tool-icon/tool-glass-tile` marked `structural: true`; the conclusion's five verdicts now all use `section/tool-intro` (TOOL 1..5 OF 5, verified in pixels). LESSON: when two rules can both be satisfied only below some N, they are not two rules, they are a bug that fires on every video wider than N. Grep the caps against the item count before blaming the model.
- 2026-08-01 best-ai-video-generator (v3 fold — a rule folded into the wrong surface) — owner: "the first link in description I want to use that template which covers the entire screen… I did mention this earlier for some video feedback." They had: the positional CTA rule (first mention = link-scrim, later = pill) was folded 2026-07-27 from test-03 into `lib/cue-rules.mjs` R_LINK_CTA and its generated body prompt. But the INTRO and CONCLUSION are authored by a separate pass with its own rulebook, and `lib/zone-rules.mjs` had zero mentions of it — and BOTH description mentions in this video were zone-authored, so the rule reached nobody. Added R_ZONE_LINK_CTA (and R_ZONE_STRUCTURAL, same reasoning). LESSON: when folding a rule, ask which PASSES author the thing it governs, not which file you happen to have open. A rule in one of two rulebooks is a rule that fires half the time.
- 2026-08-01 best-ai-video-generator (v3 fold — logo-tile cards without logos) — owner: "this motion graphic has O and B written as logo, which is very bad… Is this a bug or was that intentional?" Intentional card behaviour, wrong card choice: `enacted/promise-split` renders `(p.name)[0]` when a side carries no `logo`, because it is built for two PRODUCTS facing off. It was used for a rhetorical split ("best overall" vs "best for you"), which has no products, so it printed grey "O" and "B" letter tiles. New rule R_LOGO_TILES names the logo-tile cards and forbids them for abstract or rhetorical pairs, pointing at promise-payoff/keyword-statement/headline-chips instead. LESSON: a card's fallback path is where card-choice errors surface as visual defects; if you are supplying a required-looking field as empty, you have picked the wrong card.
- 2026-08-01 best-ai-video-generator (v3 fold — the host arrived 2:11 in) — owner: "this avatar came very late and it's kind of surprising… ideally the avatar should be coming at the starting of the video so that people are familiar with the face." Root cause chain: the intro host span was a SIDE span; side-mode cards render at 1920 and get composited into a 1200px column because `cue.sideMode` is read by `lib/render.mjs` and written by NOTHING (one reader, zero writers); the card came out clipped, so the span was deleted mid-run; `FRONT_ZONE=0.15` still passed because 15% of a 32-minute video is 4:49. Fixes: new `INTRO_HOST` constraint makes a host span inside the intro zone mandatory (the first-15% rule is explicitly called insufficient on long videos); the render join is routed to plan 175 with a mutation-proven test rather than patched in-session. LESSON: a percentage-based constraint silently loosens as videos get longer — bind the rule to the STRUCTURE (the intro zone) not to a fraction of the runtime.
- 2026-08-01 Convergence (best-ai-video-generator, v3 fold) — llm=86 approved=84 edited=~12 added=1 removed=3 typed=9 flags=0 lint-errors=0 (9 board items → 4 durable rule changes across 3 surfaces, 1 new card built, 1 card RETIRED from the library on owner instruction, 1 plan routed, 2 instance-only). **The `edited` count for this video is unmeasurable**: `cues.llm.json` was re-copied after every fix round during the original run, so `edit-delta` reports 0 edited against a baseline identical to the final file — exactly the failure the 2026-07-30 note warned about, repeated by a session that had read the warning. The snapshot discipline needs a mechanism, not a note: nothing prevents `cp cues.json cues.llm.json` from running twice.
- 2026-08-02 best-ai-video-generator (step 140's first real run — four export/assemble divergences, found by the owner in Resolve, not by a gate) — the DaVinci export had never been run on a video before. Every defect was the same shape (a rule that lives in `assemble.mjs`/`build-mix.mjs` and was re-implemented, or not implemented, in `export-timeline.mjs`), and each was invisible to the suite because the tests stubbed exactly the seam that broke. (1) **Asset URLs**: `--bundle` emitted `src="./media/x.mp4"`; FCPXML `src` is a URL and Resolve resolves no relative ones → "97 of 97 clips were not yet found". Every existing test passed `srcUrl: (f) => f`, so the URL policy — an untested closure inside `main()` — was the one thing never exercised. (2) **Audio**: SFX referenced raw samples at unity, dropping `gainDb` (108 instances 14–30 dB hot), `semi` (26 pitched), and the loop fill (`drone_low.wav` = 8s of media behind an 86.7s clip); VO lane was raw `vo.mp3` without `VO_CHAIN`. Verified by reconstructing build-mix's single-instance path and comparing peaks: pop -22.5/-22.5, drone -36.0/-36.0, exact. The consistent 3 dB vs raw samples is ffmpeg's power-preserving mono→stereo upmix, applied identically on both paths. (3) **Caption scope**: the sidecar SRT shipped all 1120 chunks though the burn skips any segment whose kind is not `screen`; now reuses `planSegments` (1120→932). Straddling chunks must be CLIPPED, not kept or dropped whole, because the burn renders per segment and an SRT entry has one range. (4) **Caption timing**: Resolve anchors a subtitle clip to its FIRST cue and discards the leading gap, so a filtered SRT starting at 25.61s played 25.6s early — the same bug had been hiding behind the unfiltered SRT's 0.4s first cue. Fixed with a 33ms zero-width anchor at 00:00:00. Verified data→pixels by extracting final.mp4 frames: 4.2s shows the card with NO caption, 26.2s shows "are going to settle, which tool" matching the SRT text exactly. LESSONS: (a) a second renderer of the same timeline WILL drift — export one shared builder (`sfxInstanceChain`, `VO_CHAIN`, `planSegments`) instead of two implementations; (b) when every test injects a stub for a seam, that seam is untested by construction, and it is usually the integration point most likely to break; (c) `loudnorm` cannot be reproduced with a fader — vo-processed and the shipped master BOTH peak at -1.4 dBFS while differing by ~10 LUFS, so the makeup is dynamic and belongs on the NLE master bus. Negative result worth keeping: **Resolve's subtitle importer refuses `.ass`** (greys out in the dialog), so per-word keyword colour is not reachable in Resolve from a sidecar — the choice is editable white captions or a pre-rendered overlay.
- 2026-08-08 consistent-ai-influencer (final-cut fold) — owner: "in the final cut it's not syncing... but in heygen it was syncing", narrowed by the owner to s07, the last clip. Root cause was NOT the 25fps HeyGen clips (0.24s of it) but `assemble` encoding all 104 concat pieces with `-t <float seconds>`: ffmpeg emits floor(dur*fps) frames, so each piece dropped its sub-frame remainder and the sum came to 1.562s of missing video against a 1230.229s master. Cumulative, hence worst at the end. Fix: `framesUntil()` carries the remainder against a running frame counter, pieces encode with `-frames:v`, plus an exact pre-encode invariant and a post-mux video-vs-audio gate. Measured 1.562s -> 0.029s; all 10 avatar spans frame-matched to within 1 frame. Also confirmed fps:25 is our template default, not a HeyGen ceiling (text_draft.create accepts and stores fps:30).
- 2026-08-08 consistent-ai-influencer (final-cut fold) — the length check `assemble` already had read `format=duration`, the CONTAINER's length, which is the longest stream in the file. A short video stream under full-length audio passes that every time. A length check that reads the container is not a length check: compare the streams that have to stay in sync with each other.
- 2026-08-08 consistent-ai-influencer (final-cut fold) — every `assemble` integration fixture used whole-second times (total 5s, total 30s, spans at whole seconds), where floor(dur*fps) is already exact, so none of them could see the bug. Proved by reintroducing the truncation: all 47 tests still passed. Added an OFF-GRID fixture (spans 1.07-4.43, 6.91-9.29, 11.53-15.17 in an 18.37s timeline) asserting an exact packet count, which the old code fails. Same shape as the 2026-07-19 solid-colour-clip blindness: when the bug class is rounding, a round fixture tests the arithmetic you already got right.
- 2026-08-08 consistent-ai-influencer (run defect, not owner feedback) — `migrateLedger` judged "already migrated" by the rename map's right-hand destinations only, so plan 199's three fresh intro-film folders (140/150/160), on neither side of a rename pair, orphaned any ledger reaching them. Invisible until this run recorded the first 150 approval. Idempotency now tests "is this a live step slug", with a guard looping over every step folder.
- 2026-08-09 consistent-ai-influencer (board defect) — owner: "approve final cut button is not working". It was working: it approved v3 on the first click and wrote final-cut.json. The tab rendered no approved state, so the click was invisible. `approved.finalCut` was already in the board-data payload but absent from the BoardData type. Fixed to `✓ final cut approved (vN)` + disabled + tooltip, matching Storyboard/Avatar. Guard: board-ui/test/approveFeedback.test.ts asserts every /approve* tab renders a ✓ and disables on an approved flag; mutation-tested by reverting the fix. Third invisible-not-broken report on this board.
