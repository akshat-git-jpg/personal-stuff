# 105 plan sound

Generates the semantic SFX placement plan (`sound.json`).

Sound is tied to what is happening on screen (e.g., cards appearing, avatars entering) rather than just being sprinkled randomly.

Optional step: a graphics-only video ships without sound planning.
