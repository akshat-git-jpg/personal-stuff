# Session Handoff — visuals-flow 130 feedback fold + intro rollback to the screenshot stage

## Where it started

Continuation of the `visuals-flow-feedback` fold (step 130, Opus-class only) for the video
`best-no-code-automation-tool`. The owner had earlier delegated the whole fix batch
("leaving this upon you"), which was executed autonomously. In the final stretch the owner
asked whether two versions of the intro existed, then asked to delete the rendered intro
and return to the screenshot/review stage.

Standing constraint for the whole session: the `i-have-adhd` output shape is active and does
NOT lapse on topic change — it ends only on "stop adhd mode" or "normal mode", after which it
must never be re-invoked. It governs text addressed to the owner; third-party copy goes
through `humanizer` instead.

## Decisions locked + what shipped

### This window — intro rollback

- **Confirmed only ONE animated intro ever existed.** `intro-film/out/intro.mp4` and
  `intro-film/renders/intro-film.mp4` were byte-identical (md5 `4c5cb7066e59d5cc8f24477b2bb0259c`),
  not two versions. The video workdir is untracked in git, so there is no history; the Recycle
  Bin held nothing relevant. The pre-update intro is unrecoverable.
- **The deleted render was a mid-fold state, not a clean "before".** Pixel evidence: b10's
  loading bars showed the `.qload` fix, but b01's subtitle still carried the old long line.
- **Archived before deleting** (the only surviving record of prior states):
  - `C:\Users\kushi\codebase\personal-stuff\pipelines\video\visuals-flow\videos\best-no-code-automation-tool\intro-film\archive\intro-2026-08-17-0005-midfold.mp4`
  - `C:\Users\kushi\codebase\personal-stuff\pipelines\video\visuals-flow\videos\best-no-code-automation-tool\intro-film\archive\review-2026-08-16-1425-pre-fold\` (59 files)
- **Deleted on owner request:** `intro-film/out/intro.mp4`, `intro-film/renders/intro-film.mp4`,
  `intro-film/renders/render-meta.json`.
- **Unblocked a gate that stopped the review.** `check-film-sync` rejected the composition with
  S3 "no beat banner comments found". The 17 banners existed but as `/* b01 0-4.32 — ...`;
  `BANNER_RE` in `lib/intro-film/check-film-sync.mjs` requires id, intent word, and an arrow:
  `/* b01 hook 0 -> 4.32 : ...`. Timings already agreed with screenplay.json — only the format was
  wrong. All 17 rewritten from screenplay.json as the source of truth. Gate now passes.
- **Re-ran `intro-review`:** 51 frames + 6 contact sheets. 1 error, 4 warnings.

### Earlier in the session — the fold itself (9 defects, 7 never reported by the owner)

- **`.qload` never rendered.** An `<i>` inside a non-flex `<span>` stays inline, so `width`/`height`
  are ignored. b10 and b11's entire device (19 seconds) painted nothing. Fixed with `display:block`.
- **The presenter never rendered in ANY review frame, for ANY film.** Three stacked causes, fixed in
  order: `review-film.mjs` never passed `standInImage`; the stand-in was 1s against a 113.667s film;
  and the real gate — the `<video>` carried no `data-duration`, so hyperframes never gave it a
  timeline window. Every composition decision about the right-hand third of the frame had been made
  against an empty panel. Fixing it immediately surfaced 4 more real defects.
- **Built a device-overlap + clearance audit into the composition** (`MIN_CLEARANCE = 40`, per-beat
  `tl.call`). It found 14 issues on first run; 11 were phantoms from Range rects measuring font
  ascent+descent rather than the line box, fixed by clamping to the element box and exempting
  text-vs-text pairs from the clearance band. Net: 3 real defects, including a nine-second
  gate/queue collision at b08 nobody had mentioned.
- **`#wall` and `.sub` were positioned against the gate's RESTING geometry.** b03 scales the gate
  1.045 about `50% 100%` and never scales it back, so its real edges differ for the rest of the film.
  `#wall` 1560 -> 1620; `.sub` 330 -> 288 -> 268.
- **The queue was b09's device shipped 9 seconds early**, running under the gate through all of b08.
  Moved its entrance to 48.2. Removing it revealed b08's intended "three faint chains" device, which
  had been built all along and hidden.
- **`TASTE-INTRO.md` grew to 17 rules** — added T16 (a tweened property that cannot apply to the
  element is invisible to everything except the frame) and T17 (position against the geometry a beat
  gives an element, not the geometry it has at rest). T13's "Enforced by" now names the audit instead
  of "author judgement, and that is currently a gap".
- **`film-assets.mjs` hardened:** `STAND_IN_SECONDS = 300`; `-framerate 1` BEFORE `-i` (the real cause
  of two silent ffmpeg hangs — `-r 1` alone is an output rate against a 25fps demuxer default and
  degenerates to `frame=0`); atomic temp-write-then-rename (a killed ffmpeg was publishing a
  moov-less mp4 under the real name, and the idempotence marker then vouched for it); EPERM retry;
  `stdio: 'ignore'`.
- `feedback.json` and `claude_status.json` were BOTH marked — the gate lies if only one is.

## Key files for next session

- `C:\Users\kushi\codebase\personal-stuff\pipelines\video\visuals-flow\videos\best-no-code-automation-tool\intro-film\film\index.html`
  — the composition. **Read this first.** 904 lines. It is NOT the state the previous session left it
  in (~557 lines): it carries a `/* ---- b04-b06 REDESIGNED 2026-08-16 (owner: ...` block and the b01
  subtitle reverted to its long form. Other fixes from the fold survived. Treat concurrent
  modification as likely and re-read before editing.
- `C:\Users\kushi\codebase\personal-stuff\pipelines\video\visuals-flow\videos\best-no-code-automation-tool\intro-film\review\REVIEW.md`
  — the fresh review report, 51 frames.
- `C:\Users\kushi\codebase\personal-stuff\pipelines\video\visuals-flow\TASTE-INTRO.md` — 17 owner-rejection rules.
- `C:\Users\kushi\codebase\personal-stuff\pipelines\video\visuals-flow\lib\intro-film\check-film-sync.mjs` — the banner-format gate.
- `C:\Users\kushi\codebase\personal-stuff\pipelines\video\visuals-flow\lib\intro-film\film-assets.mjs` — stand-in encoder, hardened.
- `C:\Users\kushi\codebase\personal-stuff\pipelines\video\visuals-flow\videos\best-no-code-automation-tool\feedback.json`
  and `claude_status.json` — the fold ledger; both must be marked together.
- Plan file: none drove this session. A future plan is routed for extracting the audit (see Deferred).
- Memory files touched: none this session.

## Running state

- Background processes: none started via `run_in_background` this session.
- Dev servers / ports: **visuals-flow board on port 4322** — http://localhost:4322/?video=best-no-code-automation-tool
  It was already running (a second `node lib/board.mjs` refused to start and reused it). To find and
  stop it: `netstat -ano | grep :4322` then `taskkill //PID <pid> //F`.
- `node.exe` pid 11792 is `local-apps-dashboard` — unrelated, leave it alone.
- Open worktrees / branches: none. Working directly on `main` in
  `C:\Users\kushi\codebase\personal-stuff`. Nothing committed this session; the video workdir is
  untracked by design.
- Backup of the composition before the banner rewrite:
  `C:\Users\kushi\.claude\jobs\a678c3b4\tmp\index.html.bak` (job-scoped, deleted with the job).

## Verification — how to confirm things still work

Run all from `C:\Users\kushi\codebase\personal-stuff\pipelines\video\visuals-flow`.

- `node lib/intro-film/check-film-sync.mjs best-no-code-automation-tool` — expect `film-sync: ok (17 beats agree)`
- `node lib/intro-film/check-taste-intro.mjs` — expect `intro taste ok — 17 rules`
- `node lib/check-rulebook.mjs` — expect `rulebook ok`
- `node lib/feedback-status.mjs best-no-code-automation-tool` — expect exit 0
- `bash run.sh best-no-code-automation-tool intro-review` — expect 51 frames, 6 contact sheets,
  and exactly 1 error (the b08 clearance below). Takes a few minutes.
- `scripts/check.sh` currently has **14 pre-existing failures unrelated to this work** — a Windows
  absolute-path join bug in `lib/` producing paths like
  `mkdir 'C:\...\videos\C:\Users\kushi\AppData\Local\Temp\film-assets-XXXX\...'`. Do not treat these
  as a regression from the fold.

## Deferred + open questions

- **Deferred — the one live error:** at b08 the subtitle `#u08` ("this same 3-step workflow, built in
  all five tools", `film/index.html` line ~440) wraps to two lines in its 1120px box, ending at y393,
  while `#q08` starts at top:430. 37px apart against a 40px minimum (T13). The film's own contract
  says subs are single-line; the fix is to shorten the line, not to move the queue. The owner was
  asked and had not answered when the session ended.
- Deferred: extract the T11+T13 clearance audit out of per-film boilerplate into a shared snippet.
  It does not carry itself forward — an absent check looks exactly like a passing one. Routed to `plans/`.
- Deferred: the film's baked-base64 logo debt.
- Deferred: log the fold via `node lib/run-log.mjs best-no-code-automation-tool 130 done --did .. --issues .. --output ..`
  and append it to `tests/TESTS.md`. Not yet done.
- Open: whether to delete `intro-film/archive/intro-2026-08-17-0005-midfold.mp4` too. The owner was
  offered this and did not answer.
- Open: who edited `film/index.html` outside this session (the b04-b06 redesign, the reverted b01
  subtitle, the non-contract banner format). If a parallel agent or session is live on this video,
  runs must be serialised — concurrent `intro-review` runs corrupt the avatar stand-in, and a corrupt
  stand-in fails silently by simply not painting the presenter.
- Open owner decisions, all pre-existing: `segments.json` still `confirmed:false`; the 286s
  "Zapier over n8n screen" recording error; ASR garbles at 978s / 1208s / 1673s; gates 150 (intro)
  and 340 (storyboard).

## Operating constraints that carry forward

- Live HeyGen: Avatar III test renders are pre-authorized. Anything metered (Avatar IV, generative
  credits) and all production renders stay owner-run with an explicit ask in conversation. Never
  submit from a cron. `engineMode` defaults to `"test"` — never flip it to `"production"` yourself.
- Step 130 feedback-fold is Opus-class ONLY.
- Never edit RULEBOOK / prompt / DESIGN / catalog / lint constants mid-run — rule changes go through
  the 130 fold.
- Never edit a symlinked skill copy; edit the source in `tooling/claude-skills/`.
- Card library and `DESIGN.md` are read-only from the intro-authoring step. Step 130 must never read
  `catalog.json`, `card-plan.json`, `cues.json`, or any card template.
- Intro items route to `TASTE-INTRO.md` / `steps/025-author-intro-film-llm/AUTHORING.md` and NEVER to
  `lib/cue-rules.mjs`, `lib/zone-rules.mjs`, `lib/zone-constants.mjs`, or `../card-library/DESIGN.md`.
  The reverse also holds.
- Commit/push only when the owner asks.

## Pick up here

Shorten `#u08` in `film/index.html` to a single line, then re-run
`bash run.sh best-no-code-automation-tool intro-review` and confirm it comes back with 0 errors.
