# 030 - write the outline

**[LLM]** &nbsp; One page: sections and headings only, no script.

`knowledge.md` -> `videos/<key>/outline.md`, following `OUTLINE-INSTRUCTIONS.md`. A table of contents that fits one screen, then one card per section: a one-line promise and three to six bullets naming what happens inside. Plus `Format:` and `Target:` lines, the intro's hook/credibility/roadmap as bullets, and one line on how the conclusion is organised. No spoken copy, no lanes, no tables. Added 2026-08-23; the one-line-per-section cap was removed 2026-08-27 at the owner's request.

**Reads:** `knowledge.md`

**Writes:** `outline.md`

---

## What happens

Read `OUTLINE-INSTRUCTIONS.md` in full and follow it exactly. Write
`videos/<key>/outline.md`.

## The rules that matter

**No spoken copy. No lanes.** Not one sentence the maker would read aloud, and no
`SAY`/`VIDEO`/`FACTS`, verdicts, blockquotes or tables. Those are all
script-plan forms, and putting them here means the two documents will drift.
**This is the boundary between the two documents** - it is not a line count.

**Three to six bullets per card**, and no prose paragraph under a heading beyond
the single promise line. A card that outgrows six bullets has become a beat.

**Section names use plain everyday words** - the words a viewer would say to a
friend. Say the name out loud; if a normal person would not, it is wrong. Phrases
are fine, sentence case not Title Case, and no industry jargon
(`The Music Bed`, `The Motion Pass`, `b-roll`). Symmetry is welcome where it
happens on its own and is never chased, because a grammatical mould gets filled
with jargon. `OUTLINE-INSTRUCTIONS.md` has the table of good and bad names, and
`TASTE.md` T8 is the standing rule.

**Never make a section out of failures.** No `When It Breaks`, no `Common
Mistakes`, no `Tips And Tricks`. A failure belongs in the phase where it happens.

**`Target:` is required**, and per `TASTE.md` T6 you push it as long as
`knowledge.md` honestly carries - then say at the gate where the ceiling is and
why. Never pad to reach a number.

**Write the credibility line as fact**, per `TASTE.md` T7. Do not hedge it and do
not ask the owner whether he has done the thing.

### Removed 2026-08-27

This section used to open with *"One line per section. Never two."* The owner
removed it: *"i need more details what we are planning inside each section so that
i can visualize"*. `test/desk-docs.test.mjs` was updated in the same change to
guard the bullet cap and the no-copy/no-lanes bans instead of the line count.

## Why it is separate from the script plan

A wrong running order costs one page here. It costs a 25-beat rewrite at 050.
